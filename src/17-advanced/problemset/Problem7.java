/**
 * Problem 07 — Range Sum Query - Immutable (LeetCode 303). Precompute a prefix-sum table and answer
 * inclusive range-sum queries in \(O(1)\) each.
 */
class Problem7 {

	/**
	 * Answers a batch of inclusive range-sum queries over an immutable array. Each query is a
	 * 2-element array {@code {l, r}}. Answers are returned in query order.
	 *
	 * @param nums the immutable array
	 * @param queries the inclusive {@code {l, r}} range queries
	 * @return the range sums, in query order
	 */
	int[] sumRanges(int[] nums, int[][] queries) {
		// TODO: build a prefix-sum table once, then answer each query in O(1).
		throw new UnsupportedOperationException("TODO: implement sumRanges");
	}
}
