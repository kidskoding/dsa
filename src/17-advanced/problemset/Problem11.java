/**
 * Problem 11 — Range Counts in Sorted Data. Sort once, then binary-search both bounds per query to
 * count readings in an inclusive value range in \(O(\log n)\).
 */
class Problem11 {

	/**
	 * Counts how many readings fall in each inclusive value range. Each query is a 2-element array
	 * {@code {lo, hi}}. Answers are returned in query order.
	 *
	 * @param readings the sensor readings
	 * @param queries the inclusive {@code {lo, hi}} value ranges
	 * @return the counts, in query order
	 */
	int[] countInRanges(int[] readings, int[][] queries) {
		// TODO: sort readings once, then binary-search the bounds per query.
		throw new UnsupportedOperationException("TODO: implement countInRanges");
	}
}
