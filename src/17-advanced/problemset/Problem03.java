/** Tier 1 — Problem 03: Range Sum with Point Updates. */
public class Problem03 {

	/**
	 * Starts from {@code initial}, applies point updates {@code updates[i] = {index, delta}}, then
	 * answers range-sum queries {@code queries[j] = {lo, hi}} (inclusive), returning one sum per
	 * query.
	 *
	 * @param initial the initial array
	 * @param updates point updates, each {@code {index, delta}}
	 * @param queries inclusive range queries, each {@code {lo, hi}}
	 * @return the range sums, one per query, in order
	 */
	public long[] rangeSumAfterUpdates(int[] initial, int[][] updates, int[][] queries) {
		// TODO: use a Fenwick tree; apply updates then answer each range query in O(log n).
		throw new UnsupportedOperationException("TODO: implement rangeSumAfterUpdates");
	}
}
