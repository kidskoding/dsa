/**
 * Problem 24 — Number of Longest Increasing Subsequence via Fenwick (LeetCode 673). Count the
 * longest strictly increasing subsequences in \(O(n \log n)\).
 */
class Problem24 {

	/**
	 * Returns the number of longest strictly increasing subsequences of {@code nums}.
	 *
	 * @param nums the input array
	 * @return the count of longest strictly increasing subsequences
	 */
	int findNumberOfLIS(int[] nums) {
		// TODO: Fenwick keyed by compressed value storing (best length, count) for smaller values.
		throw new UnsupportedOperationException("TODO: implement findNumberOfLIS");
	}
}
