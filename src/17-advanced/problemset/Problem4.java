/**
 * Problem 04 — Coordinate Compression. Map each value to its index among the sorted distinct values
 * so a Fenwick tree can be indexed by a dense range.
 */
class Problem4 {

	/**
	 * Returns the compressed coordinate of each value: {@code ranks[i]} is the index of
	 * {@code values[i]} among the sorted distinct values. Equal values receive equal ranks.
	 *
	 * @param values the values to compress
	 * @return the compressed coordinate of each input value, in input order
	 */
	int[] compress(int[] values) {
		// TODO: sort distinct values and map each input to its rank.
		throw new UnsupportedOperationException("TODO: implement compress");
	}
}
