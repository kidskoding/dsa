/**
 * Problem 05 — Count Inversions. Count index pairs {@code (i, j)} with {@code i < j} and
 * {@code nums[i] > nums[j]} in \(O(n \log n)\).
 */
class Problem5 {

	/**
	 * Counts the inversions in an array: pairs {@code (i, j)} with {@code i < j} and
	 * {@code nums[i] > nums[j]}.
	 *
	 * @param nums the input array
	 * @return the number of inversions
	 */
	long countInversions(int[] nums) {
		// TODO: count inversions via merge-sort counting or a Fenwick tree over compressed values.
		throw new UnsupportedOperationException("TODO: implement countInversions");
	}
}
