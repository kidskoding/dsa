/** Tier 1 — Problem 05: Range Add, Range Sum. */
public class Problem05 {

	/**
	 * Starts from {@code initial}, applies range-add updates {@code updates[i] = {lo, hi, delta}},
	 * then answers inclusive range-sum queries {@code queries[j] = {lo, hi}}.
	 *
	 * @param initial the initial array
	 * @param updates range-add updates, each {@code {lo, hi, delta}}
	 * @param queries inclusive range-sum queries, each {@code {lo, hi}}
	 * @return the range sums, one per query, in order
	 */
	public long[] rangeAddRangeSum(int[] initial, int[][] updates, int[][] queries) {
		// TODO: use a lazy-propagation segment tree for O(log n) updates and queries.
		throw new UnsupportedOperationException("TODO: implement rangeAddRangeSum");
	}
}
