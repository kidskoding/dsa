/**
 * Problem 03 — Build a Segment Tree for Range Minimum. Answer range-minimum queries over a static
 * array, each in \(O(\log n)\) using a segment tree.
 */
class Problem3 {

	/**
	 * Answers a batch of inclusive range-minimum queries over a static array. Each query is a
	 * 2-element array {@code {l, r}} asking for the minimum of {@code nums[l..r]}. Answers are
	 * returned in query order.
	 *
	 * @param nums the static array
	 * @param queries the inclusive {@code {l, r}} range queries
	 * @return the range minima, in query order
	 */
	int[] rangeMinimums(int[] nums, int[][] queries) {
		// TODO: build a segment tree, then answer each range-min query in O(log n).
		throw new UnsupportedOperationException("TODO: implement rangeMinimums");
	}
}
