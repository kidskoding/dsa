/**
 * Problem 08 — Range Sum Query 2D - Immutable (LeetCode 304). Precompute a 2D prefix-sum table and
 * answer rectangle-sum queries in \(O(1)\) each.
 */
class Problem8 {

	/**
	 * Answers a batch of rectangle-sum queries over an immutable matrix. Each query is a 4-element
	 * array {@code {r1, c1, r2, c2}} giving the inclusive submatrix corners. Answers are returned in
	 * query order.
	 *
	 * @param matrix the immutable matrix
	 * @param queries the {@code {r1, c1, r2, c2}} rectangle queries
	 * @return the rectangle sums, in query order
	 */
	int[] sumRegions(int[][] matrix, int[][] queries) {
		// TODO: build a 2D prefix-sum table, then answer each rectangle query in O(1).
		throw new UnsupportedOperationException("TODO: implement sumRegions");
	}
}
