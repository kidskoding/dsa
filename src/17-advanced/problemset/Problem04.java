/** Tier 1 — Problem 04: Static Range Minimum. */
public class Problem04 {

	/**
	 * Preprocesses {@code values} for O(1) range-minimum queries and answers each inclusive query
	 * {@code queries[i] = {lo, hi}}.
	 *
	 * @param values the static array
	 * @param queries inclusive range-minimum queries, each {@code {lo, hi}}
	 * @return the range minima, one per query, in order
	 */
	public int[] rangeMinima(int[] values, int[][] queries) {
		// TODO: build a sparse table and answer each query in O(1).
		throw new UnsupportedOperationException("TODO: implement rangeMinima");
	}
}
