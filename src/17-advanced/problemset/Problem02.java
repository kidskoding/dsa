/** Tier 1 — Problem 02: Stabbing Query. */
public class Problem02 {

	/**
	 * Returns whether any of the given closed intervals contains the query point {@code q}.
	 *
	 * @param lows the interval low endpoints
	 * @param highs the interval high endpoints, aligned with {@code lows}
	 * @param q the query point
	 * @return {@code true} if some interval {@code [lows[i], highs[i]]} contains {@code q}
	 */
	public boolean anyIntervalContains(int[] lows, int[] highs, int q) {
		// TODO: build an interval tree and run a stabbing query for q.
		throw new UnsupportedOperationException("TODO: implement anyIntervalContains");
	}
}
