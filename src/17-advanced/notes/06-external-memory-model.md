# External-Memory Model

## The DAM Model: \\( B \\) and \\( M \\)
*Define block size \\( B \\), memory size \\( M \\), and what a single I/O transfers.*

## Counting I/Os, Not Operations
*Why does the cost metric change when data won't fit in RAM?*

## Scanning Bound \\( O(n/B) \\)
*Argue why a linear scan costs \\( \Theta(n/B) \\) block transfers.*

## Sorting Bound
*State the \\( O\!\left(\frac{n}{B}\log_{M/B}\frac{n}{B}\right) \\) bound and the intuition behind it.*

## B-Trees as the Native Structure
*Why does fanout \\( \Theta(B) \\) give \\( O(\log_B n) \\) search I/Os?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn.*
