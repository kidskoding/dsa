# Sparse Tables

## Idempotent Operations Only
*Why does overlap-tolerant querying require \\( f(x,x)=x \\)? Which operations qualify?*

## Powers-of-Two Decomposition
*How is \\( \text{table}[k][i] \\) defined over the range of length \\( 2^k \\)?*

## \\( O(n \log n) \\) Precomputation
*Trace the doubling recurrence that fills the table.*

## \\( O(1) \\) Overlapping Query
*Show how two overlapping blocks of size \\( 2^k \\) cover any \\( [l, r] \\).*

## Logarithm Lookup
*Why precompute \\( \lfloor \log_2 \rfloor \\), and how does it speed each query?*

## Limits: No Updates
*Why are sparse tables static, and what breaks if the array changes?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn.*
