# Suffix Arrays

## Definition: Sorted Suffix Indices
*What does \\( \text{SA}[i] \\) hold, and how does it relate to sorting all suffixes?*

## Naive vs Prefix-Doubling Construction
*Contrast \\( O(n^2 \log n) \\) sorting against doubling the compared length each round.*

## Rank Arrays and Radix Sort
*How do rank pairs let you sort by \\( 2k \\)-prefixes from \\( k \\)-prefixes?*

## LCP Array
*What does the longest-common-prefix array add, and how is Kasai's method \\( O(n) \\)?*

## Pattern Search via Binary Search
*Trace locating a pattern's occurrences in \\( O(m \log n) \\).*

## Suffix Array vs Suffix Tree
*Compare space and capability trade-offs at a glance.*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn.*
