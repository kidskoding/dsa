# Interval Trees

## Keying by Low Endpoint
*Why order the underlying BST on interval left endpoints?*

## Augmenting with Subtree Max
*What does the \\( \text{max} \\) field store, and how does it summarize a subtree?*

## Overlap Search
*Trace the decision at each node: go left or right based on \\( \text{max} \\).*

## Why the Search Finds an Overlap
*Argue correctness: if an overlap exists, the descent rule never misses it.*

## Reporting All Overlaps
*How do you extend single-overlap search to enumerate every match?*

## Maintaining Max Under Rotations
*Show the local recomputation that keeps the augmentation valid.*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn.*
