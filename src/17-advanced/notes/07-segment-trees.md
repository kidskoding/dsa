# Segment Trees

## Recursive Build
*Outline how you split a node's range at the midpoint and combine children bottom-up.*

## Node Range Representation
*How does each node map to an interval \\( [lo, hi] \\)? Array layout vs pointer layout?*

## Range Query Decomposition
*Sketch the three cases (no overlap, full overlap, partial) as you descend.*

## Point vs Range Update
*Contrast updating a single leaf against updating a whole interval.*

## Associative Merge Functions
*What property must the combine operation have? Examples: sum, min, gcd.*

## Why \\( O(\log n) \\) Nodes Visited
*Argue why a query touches only \\( O(\log n) \\) canonical segments.*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn.*
