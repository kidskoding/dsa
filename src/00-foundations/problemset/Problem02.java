/** Tier 1 — Problem 02: Geometric sum. */
public class Problem02 {

	/**
	 * Computes {@code sum_{i=0}^{k-1} a·r^i} for a given first term, ratio, and number of terms,
	 * handling the {@code r == 1} case.
	 *
	 * @param a the first term
	 * @param r the common ratio
	 * @param k the number of terms, k &ge; 0
	 * @return the value of the geometric sum
	 */
	public double geometricSum(double a, double r, int k) {
		// TODO: apply the closed form, special-casing r == 1.
		throw new UnsupportedOperationException("TODO: implement geometricSum");
	}
}
