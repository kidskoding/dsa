/** Foundations — Problem 2: Geometric Series Sum. */
class Problem2 {

	/**
	 * Returns the sum of the geometric series {@code a + a*r + ... + a*r^(k-1)}, handling the
	 * {@code r == 1} case where the sum is {@code a*k}.
	 *
	 * @param a the first term
	 * @param r the integer ratio
	 * @param k the number of terms
	 * @return the series sum
	 */
	long geometricSum(long a, long r, int k) {
		// TODO: accumulate or use the closed form, handling r == 1.
		throw new UnsupportedOperationException("TODO: implement geometricSum");
	}
}
