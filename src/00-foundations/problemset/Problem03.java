/** Tier 1 — Problem 03: Floor log base 2. */
public class Problem03 {

	/**
	 * Returns {@code floor(log_2(n))} for an integer {@code n >= 1} using only integer operations (no
	 * floating point).
	 *
	 * @param n a positive integer
	 * @return the floor of the base-2 logarithm of n
	 */
	public int floorLog2(long n) {
		// TODO: count halvings (or leading-zero bits) without floating point.
		throw new UnsupportedOperationException("TODO: implement floorLog2");
	}
}
