/** Foundations — Problem 10: Log Factorial Cost. */
class Problem10 {

	/**
	 * Returns {@code log2(n!)} computed as {@code sum_{i=1}^{n} log2(i)}, growing as
	 * {@code Theta(n log n)}; the result is {@code 0.0} for {@code n <= 1}.
	 *
	 * @param n the factorial argument, at least 0
	 * @return the base-2 log of n factorial
	 */
	double logFactorial(int n) {
		// TODO: accumulate log base 2 of each i in 1..n.
		throw new UnsupportedOperationException("TODO: implement logFactorial");
	}
}
