/** Tier 1 — Problem 01: Count basic operations. */
public class Problem01 {

	/**
	 * Returns the exact number of times the innermost statement runs in a doubly nested loop where
	 * the inner loop runs from the outer index up to {@code n}. The result should match a simulated
	 * count.
	 *
	 * @param n the loop bound
	 * @return the number of innermost statement executions
	 */
	public long countOperations(int n) {
		// TODO: derive the closed form for the nested-loop iteration count.
		throw new UnsupportedOperationException("TODO: implement countOperations");
	}
}
