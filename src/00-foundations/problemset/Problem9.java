/** Foundations — Problem 9: Reduce a Fraction. */
class Problem9 {

	/**
	 * Reduces {@code numerator/denominator} to lowest terms via the Euclidean algorithm, returning
	 * {@code [num, den]} with the sign normalized so {@code den} is positive.
	 *
	 * @param numerator the numerator
	 * @param denominator the nonzero denominator
	 * @return the reduced fraction as a two-element array
	 */
	long[] reduce(long numerator, long denominator) {
		// TODO: divide by the gcd and normalize the sign onto the numerator.
		throw new UnsupportedOperationException("TODO: implement reduce");
	}
}
