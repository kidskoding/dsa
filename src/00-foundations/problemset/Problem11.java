/** Foundations — Problem 11: Recursion Tree Total Work. */
class Problem11 {

	/**
	 * For a power-of-two {@code n}, returns the total work in the recursion tree of
	 * {@code T(n) = 2T(n/2) + n}, equal to {@code n*(log2(n) + 1)}.
	 *
	 * @param n a power of two, at least 1
	 * @return the total work across all nodes
	 */
	long totalWork(long n) {
		// TODO: multiply n by the number of levels (log2(n) + 1).
		throw new UnsupportedOperationException("TODO: implement totalWork");
	}
}
