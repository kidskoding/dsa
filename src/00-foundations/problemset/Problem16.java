/** Foundations — Problem 16: Exponential Overtakes Polynomial. */
class Problem16 {

	/**
	 * Returns the smallest positive integer {@code n} such that {@code 2^n > n^k}. Compare without
	 * overflowing, e.g. via logarithms.
	 *
	 * @param k the polynomial exponent, at least 1
	 * @return the smallest n where 2^n strictly exceeds n^k
	 */
	long smallestN(int k) {
		// TODO: scan n upward comparing n*log(2) to k*log(n).
		throw new UnsupportedOperationException("TODO: implement smallestN");
	}
}
