/** Foundations — Problem 8: Trailing Zeros of a Factorial. */
class Problem8 {

	/**
	 * Returns the number of trailing zeros in {@code n!}, equal to
	 * {@code floor(n/5) + floor(n/25) + ...}.
	 *
	 * @param n the factorial argument, at least 0
	 * @return the count of trailing zeros
	 */
	long trailingZeros(long n) {
		// TODO: sum the floor divisions by successive powers of 5.
		throw new UnsupportedOperationException("TODO: implement trailingZeros");
	}
}
