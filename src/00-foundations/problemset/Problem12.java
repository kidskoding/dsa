/** Foundations — Problem 12: Binary Counter Bit Flips. */
class Problem12 {

	/**
	 * Returns the total number of bit flips when incrementing a binary counter from zero {@code k}
	 * times. The amortized cost approaches 2 flips per increment.
	 *
	 * @param k the number of increments
	 * @return the total bit flips
	 */
	long totalBitFlips(long k) {
		// TODO: sum the flips per increment, or use the closed form k bit i flips floor(k/2^i).
		throw new UnsupportedOperationException("TODO: implement totalBitFlips");
	}
}
