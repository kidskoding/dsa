/** Foundations — Problem 1: Nested Loop Operation Count. */
class Problem1 {

	/**
	 * Returns the exact number of times the innermost statement runs in a doubly nested loop where
	 * the inner index runs from the outer index up to {@code n}. This is the triangular number
	 * {@code n(n+1)/2}.
	 *
	 * @param n the loop bound
	 * @return the number of innermost executions
	 */
	long countOperations(int n) {
		// TODO: return the triangular number for n.
		throw new UnsupportedOperationException("TODO: implement countOperations");
	}
}
