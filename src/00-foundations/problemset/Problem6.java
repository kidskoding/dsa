/** Foundations — Problem 6: Dynamic Array Doubling Copy Cost. */
class Problem6 {

	/**
	 * Simulates {@code n} pushes into a dynamic array that starts at capacity 1 and doubles when
	 * full, returning the total number of element copies performed across all resizes.
	 *
	 * @param n the number of pushes
	 * @return the total element copies
	 */
	long totalCopies(int n) {
		// TODO: copy the current size whenever capacity must double.
		throw new UnsupportedOperationException("TODO: implement totalCopies");
	}
}
