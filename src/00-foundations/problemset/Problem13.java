/** Foundations — Problem 13: Digit Power Summation. */
class Problem13 {

	/**
	 * For every integer from 1 to {@code n}, raises each decimal digit to the power {@code p} and
	 * returns the accumulated total.
	 *
	 * @param n the upper bound, at least 1
	 * @param p the digit exponent
	 * @return the total of all digit-powers
	 */
	long digitPowerSum(int n, int p) {
		// TODO: for each number, raise each digit to p and accumulate.
		throw new UnsupportedOperationException("TODO: implement digitPowerSum");
	}
}
