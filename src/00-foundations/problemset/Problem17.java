/** Foundations — Problem 17: Tribonacci Modulo. */
class Problem17 {

	/**
	 * Returns {@code T(k) mod 1000000007} for the recurrence
	 * {@code T(k) = T(k-1) + T(k-2) + T(k-3)} with {@code T(0)=0, T(1)=1, T(2)=1}.
	 *
	 * @param k the index, at least 0
	 * @return T(k) modulo 1000000007
	 */
	long tribonacci(int k) {
		// TODO: iterate the recurrence under the modulus.
		throw new UnsupportedOperationException("TODO: implement tribonacci");
	}
}
