/**
 * Problem 3 — Heapsort.
 *
 * <p>Given an integer array, sort it ascending in place using heapsort: build a max-heap over the
 * whole array, then repeatedly swap the root to the end of the active region and sift the new root
 * down over the shrunken region.
 */
final class Problem3 {

	private Problem3() {}

	/**
	 * Sorts {@code array} ascending in place using heapsort.
	 *
	 * @param array the array to sort, modified in place
	 */
	static void heapsort(int[] array) {
		// TODO: build a max-heap, then extract the maximum to the end repeatedly.
		throw new UnsupportedOperationException("TODO: implement heapsort");
	}
}
