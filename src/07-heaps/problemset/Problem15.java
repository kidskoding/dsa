/**
 * Problem 15 — Reorganize String.
 *
 * <p>Rearrange the characters of {@code s} so that no two adjacent characters are equal, returning
 * any valid arrangement or {@code ""} if none exists. Greedily place the most frequent remaining
 * character using a max-heap.
 */
final class Problem15 {

	private Problem15() {}

	/**
	 * Returns a rearrangement of {@code s} with no two equal adjacent characters, or {@code ""}.
	 *
	 * @param s the input string
	 * @return a valid rearrangement, or the empty string if impossible
	 */
	static String reorganizeString(String s) {
		// TODO: repeatedly emit the most frequent character that differs from the previous one.
		throw new UnsupportedOperationException("TODO: implement reorganizeString");
	}
}
