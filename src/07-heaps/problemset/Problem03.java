/**
 * Problem 03 — Heapsort.
 *
 * <p>Sort an integer array ascending, in place, using heapsort: build a max-heap, then repeatedly
 * swap the maximum (root) to the end and sift the new root down over the shrinking heap.
 */
public final class Problem03 {

	private Problem03() {}

	/**
	 * Sorts {@code array} ascending in place using heapsort.
	 *
	 * @param array the array to sort in place
	 */
	public static void heapsort(int[] array) {
		// TODO: build a max-heap, then extract the max into the tail repeatedly.
		throw new UnsupportedOperationException("TODO: implement heapsort");
	}
}
