/**
 * Problem 02 — Is it a min-heap?
 *
 * <p>Given an integer array using the standard array-as-complete-binary-tree layout, decide whether
 * it satisfies the min-heap property (every parent {@code <=} each of its children).
 */
public final class Problem02 {

	private Problem02() {}

	/**
	 * Returns whether {@code array} satisfies the min-heap property.
	 *
	 * @param array the array to test
	 * @return {@code true} if every parent is {@code <=} each of its children
	 */
	public static boolean isMinHeap(int[] array) {
		// TODO: verify array[parent] <= array[child] for every child index.
		throw new UnsupportedOperationException("TODO: implement isMinHeap");
	}
}
