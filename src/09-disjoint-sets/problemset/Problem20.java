/** Problem 20: Advent Grid Flood. */
public class Problem20 {

	/**
	 * Given a {@code rows x cols} grid of characters (one string per row), returns the size of the
	 * single largest region, where a region is a maximal group of orthogonally adjacent cells
	 * holding the same character. Map each cell to an index, union equal adjacent cells, then return
	 * the maximum component size.
	 *
	 * @param grid the character grid, one non-empty string per row
	 * @return the cell count of the largest region
	 */
	int largestRegion(String[] grid) {
		// TODO: union equal adjacent cells (right and down), then return the max component size.
		throw new UnsupportedOperationException("TODO: implement largestRegion");
	}
}
