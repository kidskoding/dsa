/** Problem 7: Find if Path Exists in Graph. */
public class Problem7 {

	/**
	 * Given a bidirectional graph with {@code n} vertices labelled {@code 0..n-1} and a list of
	 * {@code edges}, returns {@code true} if a path exists between {@code source} and
	 * {@code destination}. Union every edge, then compare the representatives of the two vertices.
	 *
	 * @param n the number of vertices, n &ge; 1
	 * @param edges the undirected edges, each a two-element array
	 * @param source the start vertex
	 * @param destination the target vertex
	 * @return true if a path exists, false otherwise
	 */
	boolean validPath(int n, int[][] edges, int source, int destination) {
		// TODO: union all edges, then return whether source and destination share a root.
		throw new UnsupportedOperationException("TODO: implement validPath");
	}
}
