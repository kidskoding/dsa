/** Problem 8: Satisfiability of Equality Equations. */
public class Problem8 {

	/**
	 * Given an array of {@code equations}, each of the form {@code "a==b"} or {@code "a!=b"} over
	 * single lowercase variables, returns {@code true} if some assignment satisfies them all. Union
	 * the {@code ==} pairs, then verify that no {@code !=} pair lies in the same set.
	 *
	 * @param equations the equality and inequality equations, each a 4-character string
	 * @return true if all equations can hold simultaneously, false otherwise
	 */
	boolean equationsPossible(String[] equations) {
		// TODO: union the == pairs, then check no != pair shares a root.
		throw new UnsupportedOperationException("TODO: implement equationsPossible");
	}
}
