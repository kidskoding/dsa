/** Problem 3: Redundant Connection. */
public class Problem3 {

	/**
	 * Given {@code edges} forming a tree plus exactly one extra edge (nodes labelled {@code 1..n}),
	 * returns the edge that can be removed so the remaining graph is a tree. Union endpoints in
	 * input order; the first edge whose endpoints are already connected is the answer.
	 *
	 * @param edges the edges in insertion order, each a two-element array
	 * @return the redundant edge, as a two-element array
	 */
	int[] findRedundantConnection(int[][] edges) {
		// TODO: union endpoints in order; return the first edge whose endpoints are already joined.
		throw new UnsupportedOperationException("TODO: implement findRedundantConnection");
	}
}
