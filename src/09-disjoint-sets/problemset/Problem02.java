/** Tier 1 — Problem 02: Detect a cycle in an undirected graph. */
public class Problem02 {

	/**
	 * Given {@code n} nodes and a list of undirected {@code edges}, returns {@code true} if adding
	 * the edges to a union-find ever unions two already-connected endpoints (i.e. the graph contains
	 * a cycle).
	 *
	 * @param n the number of nodes, n &ge; 0
	 * @param edges the undirected edges, each a two-element array
	 * @return {@code true} if the graph contains a cycle
	 */
	public boolean hasCycle(int n, int[][] edges) {
		// TODO: process edges; if both endpoints already share a root, report a cycle.
		throw new UnsupportedOperationException("TODO: implement hasCycle");
	}
}
