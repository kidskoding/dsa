/** Problem 5: Are All Elements Connected? */
public class Problem5 {

	/**
	 * Given {@code n} elements labelled {@code 0..n-1} and a list of {@code unions}, returns
	 * {@code true} if every element ends up in a single set after applying all unions.
	 *
	 * @param n the number of elements, n &ge; 1
	 * @param unions the union operations, each a two-element array
	 * @return true if all elements are connected, false otherwise
	 */
	boolean allConnected(int n, int[][] unions) {
		// TODO: apply the unions, then return whether exactly one distinct root remains.
		throw new UnsupportedOperationException("TODO: implement allConnected");
	}
}
