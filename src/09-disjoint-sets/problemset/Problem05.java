/** Tier 1 — Problem 05: Are all elements connected? */
public class Problem05 {

	/**
	 * After applying a list of {@code unions} over {@code n} elements, returns {@code true} if every
	 * element ends up in a single set.
	 *
	 * @param n the number of elements, n &ge; 0
	 * @param unions the union operations, each a two-element array
	 * @return {@code true} if all elements share one set
	 */
	public boolean allConnected(int n, int[][] unions) {
		// TODO: apply the unions, then check that exactly one distinct representative remains.
		throw new UnsupportedOperationException("TODO: implement allConnected");
	}
}
