/** Problem 1: Count Connected Components. */
public class Problem1 {

	/**
	 * Given {@code n} nodes labelled {@code 0..n-1} and a list of undirected {@code edges} (each
	 * {@code [u, v]}), returns the number of connected components by unioning the endpoints and
	 * counting distinct representatives.
	 *
	 * @param n the number of nodes, n &ge; 1
	 * @param edges the undirected edges, each a two-element array
	 * @return the number of connected components
	 */
	int countComponents(int n, int[][] edges) {
		// TODO: union each edge's endpoints, then count distinct roots.
		throw new UnsupportedOperationException("TODO: implement countComponents");
	}
}
