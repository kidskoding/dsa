/** Tier 1 — Problem 03: Redundant connection. */
public class Problem03 {

	/**
	 * Given a list of {@code edges} that forms a tree plus exactly one extra edge, returns the last
	 * edge (in input order) whose endpoints are already connected when it is processed.
	 *
	 * @param edges the edges in input order, each a two-element array of 1-indexed nodes
	 * @return the redundant edge {@code [u, v]}
	 */
	public int[] findRedundantConnection(int[][] edges) {
		// TODO: union edges in order; return the first edge whose endpoints already share a root.
		throw new UnsupportedOperationException("TODO: implement findRedundantConnection");
	}
}
