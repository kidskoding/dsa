/** Problem 2: Detect a Cycle in an Undirected Graph. */
public class Problem2 {

	/**
	 * Given {@code n} nodes labelled {@code 0..n-1} and a list of undirected {@code edges},
	 * returns {@code true} if the graph contains a cycle. Process edges with a union-find; an edge
	 * whose endpoints already share a representative closes a cycle.
	 *
	 * @param n the number of nodes, n &ge; 1
	 * @param edges the undirected edges, each a two-element array
	 * @return true if the graph contains a cycle, false otherwise
	 */
	boolean hasCycle(int n, int[][] edges) {
		// TODO: union endpoints; if an edge's endpoints are already connected, return true.
		throw new UnsupportedOperationException("TODO: implement hasCycle");
	}
}
