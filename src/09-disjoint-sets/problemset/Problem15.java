/** Problem 15: Redundant Connection (Last Removable Edge). */
public class Problem15 {

	/**
	 * Given {@code edges} of a network that began as a tree plus exactly one extra lane (forming a
	 * single cycle), returns the edge whose removal restores a tree. If several edges lie on the
	 * cycle, returns the one appearing last in the input. Union endpoints in order; the first edge
	 * whose endpoints are already connected is the answer.
	 *
	 * @param edges the edges in insertion order, each a two-element array
	 * @return the edge to remove, as a two-element array
	 */
	int[] findRemovableLane(int[][] edges) {
		// TODO: union endpoints in order; return the first edge whose endpoints are already joined.
		throw new UnsupportedOperationException("TODO: implement findRemovableLane");
	}
}
