/** Problem 14: The Earliest Moment When Everyone Become Friends. */
public class Problem14 {

	/**
	 * Given {@code n} people labelled {@code 0..n-1} and {@code logs} (each
	 * {@code [timestamp, a, b]}), returns the earliest timestamp at which everyone is connected, or
	 * {@code -1} if that never happens. Sort logs by timestamp, union friends, and return the
	 * timestamp of the union that reduces the component count to one.
	 *
	 * @param n the number of people, n &ge; 2
	 * @param logs the friendship logs, each a three-element array {@code [timestamp, a, b]}
	 * @return the earliest timestamp at which everyone is connected, or -1
	 */
	int earliestAcq(int n, int[][] logs) {
		// TODO: sort by timestamp, union, and return the timestamp that drops components to 1.
		throw new UnsupportedOperationException("TODO: implement earliestAcq");
	}
}
