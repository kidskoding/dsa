/** Tier 1 — Problem 04: Largest set size. */
public class Problem04 {

	/**
	 * Given {@code n} elements and a sequence of {@code unions} (each {@code [a, b]}), returns the
	 * size of the largest resulting set after all unions are applied.
	 *
	 * @param n the number of elements, n &ge; 0
	 * @param unions the union operations, each a two-element array
	 * @return the size of the largest set
	 */
	public int largestSetSize(int n, int[][] unions) {
		// TODO: track per-set sizes while unioning; return the maximum size seen.
		throw new UnsupportedOperationException("TODO: implement largestSetSize");
	}
}
