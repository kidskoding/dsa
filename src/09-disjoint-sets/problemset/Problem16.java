/** Problem 16: Smallest String With Swaps. */
public class Problem16 {

	/**
	 * Given a string {@code s} and {@code pairs} of swappable index pairs, returns the
	 * lexicographically smallest string obtainable by repeated swaps. Indices connected by swaps
	 * form a group; within a group, characters can be freely rearranged. Union the pairs, then sort
	 * each group's characters into its sorted indices.
	 *
	 * @param s the input string
	 * @param pairs the swappable index pairs, each a two-element array
	 * @return the lexicographically smallest reachable string
	 */
	String smallestStringWithSwaps(String s, int[][] pairs) {
		// TODO: union index pairs, then place each group's sorted chars at its sorted indices.
		throw new UnsupportedOperationException("TODO: implement smallestStringWithSwaps");
	}
}
