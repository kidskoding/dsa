/** Problem 4: Largest Set Size. */
public class Problem4 {

	/**
	 * Given {@code n} elements labelled {@code 0..n-1} and a sequence of {@code unions} (each
	 * {@code [a, b]}), returns the size of the largest resulting set after applying all unions.
	 *
	 * @param n the number of elements, n &ge; 1
	 * @param unions the union operations, each a two-element array
	 * @return the size of the largest set
	 */
	int largestSetSize(int n, int[][] unions) {
		// TODO: union with size tracking, then return the maximum set size over all roots.
		throw new UnsupportedOperationException("TODO: implement largestSetSize");
	}
}
