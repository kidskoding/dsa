# Path Compression

## Flattening the Tree
*How does pointing visited nodes directly at the root shorten future \\( \text{find} \\) paths?*

## Iterative vs. Recursive Find
*Compare the two-pass iterative version with the recursive one-pass implementation.*

## Path Splitting & Path Halving
*What are these lighter-weight alternatives, and how do they differ from full compression?*

## Effect on Amortized Cost
*How does compression alone change the per-operation cost over a sequence of finds?*

## Synergy with Union by Rank
*Why does combining compression with rank/size yield the near-constant bound neither achieves alone?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
