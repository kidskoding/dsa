# Amortized Analysis & the Inverse Ackermann Function

## The Ackermann Function
*Recall how \\( A(m, n) \\) grows explosively — what makes it the canonical fast-growing function?*

## The \\( \alpha(n) \\) Function
*Define the inverse \\( \alpha(n) \\) and convince yourself it is \\( \leq 4 \\) for any practical \\( n \\).*

## Why It's Near-Constant
*Why do we treat \\( \alpha(n) \\) as effectively constant despite not being truly \\( O(1) \\)?*

## Tarjan's Bound
*State the \\( O(m\,\alpha(n)) \\) result for \\( m \\) operations and what optimizations it assumes.*

## Amortization Technique (Potential / Accounting)
*How does a potential function spread expensive finds across cheap ones?*

## Worst Case vs. Amortized Distinction
*Why can a single operation still be costly even when the amortized bound is near-constant?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
