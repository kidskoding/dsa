# Union-Find

## Disjoint-Set Abstraction
*What problem does maintaining a partition of \\( n \\) elements into disjoint sets solve?*

## Representative Elements & Find
*How does each set get a canonical representative, and how does \\( \text{find} \\) return it?*

## Union by Linking Roots
*Why must union operate on roots rather than arbitrary elements?*

## Parent-Pointer Forest Representation
*How is the structure encoded as an array of parent pointers, and what marks a root?*

## Naive Cost Without Optimizations
*Why can an unoptimized tree degrade to \\( O(n) \\) per operation?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
