/** Problem 10 — Container With Most Water. See {@code PROBLEM_SET.md}. */
class Problem10 {

	/**
	 * Returns the maximum area {@code min(h[i], h[j]) * (j - i)} over all index pairs in {@code h}.
	 *
	 * @param h the line heights
	 * @return the maximum container area
	 */
	int maxArea(int[] h) {
		// TODO: two pointers from both ends, move the shorter wall inward.
		throw new UnsupportedOperationException("TODO: implement maxArea");
	}
}
