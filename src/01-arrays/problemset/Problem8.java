/** Problem 8 — Majority Element (Boyer–Moore). See {@code PROBLEM_SET.md}. */
class Problem8 {

	/**
	 * Returns the element appearing more than {@code n/2} times in {@code a} (guaranteed to exist).
	 *
	 * @param a a non-empty array with a majority element
	 * @return the majority element
	 */
	int majorityElement(int[] a) {
		// TODO: Boyer–Moore voting — candidate and count.
		throw new UnsupportedOperationException("TODO: implement majorityElement");
	}
}
