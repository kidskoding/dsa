/** Problem 2 — Rotate Array Left. See {@code PROBLEM_SET.md}. */
class Problem2 {

	/**
	 * Rotates {@code nums} to the left by {@code k} positions in place, where {@code k} may exceed
	 * the length, and returns the same array reference.
	 *
	 * @param nums the array to rotate
	 * @param k the number of left positions to rotate by ({@code k >= 0})
	 * @return the same array reference, rotated
	 */
	int[] rotateLeft(int[] nums, int k) {
		// TODO: reduce k modulo n, then reverse-based or cyclic rotation.
		throw new UnsupportedOperationException("TODO: implement rotateLeft");
	}
}
