/** Problem 16 — Caravan Fuel (Gas Station). See {@code PROBLEM_SET.md}. */
class Problem16 {

	/**
	 * Returns the first starting index from which a full circular loop is possible, or -1.
	 *
	 * @param gas fuel provided at each station
	 * @param cost fuel burned driving from each station to the next
	 * @return the starting index, or -1 if no loop is possible
	 */
	int canCompleteCircuit(int[] gas, int[] cost) {
		// TODO: if total gas >= total cost, the start is where running tank dips lowest+1.
		throw new UnsupportedOperationException("TODO: implement canCompleteCircuit");
	}
}
