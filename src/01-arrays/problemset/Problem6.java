/** Problem 6 — Remove Duplicates from Sorted Array. See {@code PROBLEM_SET.md}. */
class Problem6 {

	/**
	 * Compacts the sorted array {@code a} in place so each value appears once, and returns the new
	 * logical length {@code k} (the first {@code k} slots hold the unique values in order).
	 *
	 * @param a a sorted (ascending) array
	 * @return the count of unique values
	 */
	int removeDuplicates(int[] a) {
		// TODO: slow/fast write index advancing on new values.
		throw new UnsupportedOperationException("TODO: implement removeDuplicates");
	}
}
