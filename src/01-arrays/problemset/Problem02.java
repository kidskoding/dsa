/** Problem 02 — Rotate Left by K. See {@code PROBLEM_SET.md}. */
public class Problem02 {

	/**
	 * Rotates {@code a} left by {@code k} positions in place; {@code k} may exceed the length.
	 *
	 * @param a the array to rotate in place
	 * @param k the number of positions to rotate left; may be larger than {@code a.length}
	 * @return the same array reference, now rotated
	 */
	public int[] rotateLeft(int[] a, int k) {
		// TODO: normalize k mod length, then reverse three ranges (or juggle).
		throw new UnsupportedOperationException("TODO: implement rotateLeft");
	}
}
