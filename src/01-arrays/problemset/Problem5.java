/** Problem 5 — Maximum Subarray (Kadane). See {@code PROBLEM_SET.md}. */
class Problem5 {

	/**
	 * Returns the maximum sum of any contiguous, non-empty subarray of {@code a}.
	 *
	 * @param a a non-empty array (may include negatives)
	 * @return the maximum contiguous subarray sum
	 */
	int maxSubarray(int[] a) {
		// TODO: Kadane's recurrence cur = max(a[i], cur + a[i]).
		throw new UnsupportedOperationException("TODO: implement maxSubarray");
	}
}
