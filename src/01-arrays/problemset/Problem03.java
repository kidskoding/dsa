/** Problem 03 — Two Sum (Sorted). See {@code PROBLEM_SET.md}. */
public class Problem03 {

	/**
	 * Finds two indices in the sorted array {@code a} whose values sum to {@code target}.
	 *
	 * @param a a sorted array of integers
	 * @param target the desired pair sum
	 * @return a two-element {@code int[]} of indices, or {@code {-1, -1}} if no pair exists
	 */
	public int[] twoSum(int[] a, int target) {
		// TODO: converging two-pointer scan from both ends.
		throw new UnsupportedOperationException("TODO: implement twoSum");
	}
}
