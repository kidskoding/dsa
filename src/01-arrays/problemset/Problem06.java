/** Problem 06 — Remove Duplicates From Sorted. See {@code PROBLEM_SET.md}. */
public class Problem06 {

	/**
	 * Compacts the sorted array {@code a} so each value appears once, in place, and returns the new
	 * logical length. Elements beyond the returned length are unspecified.
	 *
	 * @param a a sorted array, compacted in place
	 * @return the number of unique elements (the new logical length)
	 */
	public int removeDuplicates(int[] a) {
		// TODO: slow/fast write-pointer compaction.
		throw new UnsupportedOperationException("TODO: implement removeDuplicates");
	}
}
