/** Problem 20 — Report Repair (two entries summing to target). See {@code PROBLEM_SET.md}. */
class Problem20 {

	/**
	 * Returns the product of the two distinct entries of {@code a} that sum to {@code target}
	 * (exactly one such pair exists).
	 *
	 * @param a the expense report
	 * @param target the target sum
	 * @return the product of the matching pair
	 */
	long reportRepair(int[] a, int target) {
		// TODO: hash set of seen values, or sort and use two pointers.
		throw new UnsupportedOperationException("TODO: implement reportRepair");
	}
}
