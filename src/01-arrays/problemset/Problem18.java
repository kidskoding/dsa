/** Problem 18 — Seismic Peaks. See {@code PROBLEM_SET.md}. */
class Problem18 {

	/**
	 * Returns the number of interior indices {@code i} (with {@code 0 < i < n-1}) where
	 * {@code a[i-1] < a[i] > a[i+1]}.
	 *
	 * @param a the readings
	 * @return the count of peaks
	 */
	int countPeaks(int[] a) {
		// TODO: scan interior indices and test the strict peak condition.
		throw new UnsupportedOperationException("TODO: implement countPeaks");
	}
}
