/** Problem 23 — Warehouse Forklift. See {@code PROBLEM_SET.md}. */
class Problem23 {

	/**
	 * Returns the number of drops needed to move all crates, where the forklift drops its current
	 * load whenever adding the next crate would exceed capacity {@code L}; the final load counts.
	 *
	 * @param w the crate weights
	 * @param L the forklift capacity ({@code w[i] <= L})
	 * @return the number of drops
	 */
	int drops(int[] w, long L) {
		// TODO: greedy running load; start a new load when adding would exceed L.
		throw new UnsupportedOperationException("TODO: implement drops");
	}
}
