/** Problem 24 — Maximum Sum of Window Size K. See {@code PROBLEM_SET.md}. */
class Problem24 {

	/**
	 * Returns the maximum sum of any contiguous subarray of {@code a} of length exactly {@code k}.
	 *
	 * @param a the input array
	 * @param k the fixed window size ({@code 1 <= k <= a.length})
	 * @return the maximum window sum
	 */
	int maxWindowSum(int[] a, int k) {
		// TODO: slide a fixed window, adding the entering and subtracting the leaving element.
		throw new UnsupportedOperationException("TODO: implement maxWindowSum");
	}
}
