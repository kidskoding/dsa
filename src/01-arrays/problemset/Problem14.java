/** Problem 14 — Ledger Audit (prefix-sum range queries). See {@code PROBLEM_SET.md}. */
class Problem14 {

	/**
	 * Answers each range query {@code [l, r]} with the net change over days {@code l..r} inclusive
	 * of {@code d}, using prefix sums.
	 *
	 * @param d the daily balance changes
	 * @param queries an array of {@code [l, r]} pairs
	 * @return an array of per-query net changes
	 */
	long[] rangeSums(int[] d, int[][] queries) {
		// TODO: build a prefix-sum array, answer each query in O(1).
		throw new UnsupportedOperationException("TODO: implement rangeSums");
	}
}
