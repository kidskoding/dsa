/** Problem 3 — Two Sum II (Sorted). See {@code PROBLEM_SET.md}. */
class Problem3 {

	/**
	 * Returns the two 0-based indices {@code [i, j]} with {@code i < j} whose values in the sorted
	 * array {@code a} sum to {@code target}, or {@code [-1, -1]} if none.
	 *
	 * @param a a sorted (ascending) array
	 * @param target the target sum
	 * @return the index pair, or {@code [-1, -1]}
	 */
	int[] twoSum(int[] a, int target) {
		// TODO: two pointers moving inward from both ends.
		throw new UnsupportedOperationException("TODO: implement twoSum");
	}
}
