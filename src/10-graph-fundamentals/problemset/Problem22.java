/** Applied — Problem 22: Shortest 8-directional path in a binary matrix. */
class Problem22 {

	/**
	 * Returns the length (in visited cells) of the shortest 8-directional clear path from
	 * {@code (0,0)} to {@code (n-1,n-1)}, or {@code -1} if none exists.
	 *
	 * @param grid the {@code n x n} grid of {@code 0} (clear) and {@code 1} (blocked)
	 * @return the shortest path length, or {@code -1}
	 */
	int shortestPathBinaryMatrix(int[][] grid) {
		// TODO: BFS over 8 directions from the top-left clear cell.
		throw new UnsupportedOperationException("TODO: implement shortestPathBinaryMatrix");
	}
}
