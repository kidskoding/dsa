/** Foundational — Problem 4: Detect a cycle in an undirected graph. */
class Problem4 {

	/**
	 * Reports whether an undirected graph contains a cycle.
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the undirected edges as {@code [u, v]} pairs
	 * @return {@code true} if a cycle exists, otherwise {@code false}
	 */
	boolean hasCycle(int vertexCount, int[][] edges) {
		// TODO: DFS/union-find; a back edge to a visited non-parent vertex marks a cycle.
		throw new UnsupportedOperationException("TODO: implement hasCycle");
	}
}
