/** Applied — Problem 11: Count provinces from an adjacency matrix. */
class Problem11 {

	/**
	 * Counts the provinces (connected components) described by an {@code n x n} symmetric
	 * adjacency matrix.
	 *
	 * @param isConnected the adjacency matrix; {@code isConnected[i][j] == 1} links cities
	 * @return the number of provinces
	 */
	int findCircleNum(int[][] isConnected) {
		// TODO: traverse the matrix as a graph and count connected components.
		throw new UnsupportedOperationException("TODO: implement findCircleNum");
	}
}
