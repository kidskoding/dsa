/** Tier 1 — Problem 01: Build the shared graph and report every vertex degree. */
public class Problem01 {

	/**
	 * Builds the shared {@code Graph} from an undirected edge list and returns the degree of every
	 * vertex.
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the undirected edges as {@code [u, v]} pairs
	 * @return an array where entry {@code i} is the degree of vertex {@code i}
	 */
	public int[] degrees(int vertexCount, int[][] edges) {
		// TODO: build the undirected Graph, then read each vertex's degree.
		throw new UnsupportedOperationException("TODO: implement degrees");
	}
}
