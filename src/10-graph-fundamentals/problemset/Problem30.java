/** Applied — Problem 30: Minimum height tree roots. */
class Problem30 {

	/**
	 * Returns the root labels (one or two centroids) that minimize the tree's height, ascending.
	 *
	 * @param n the number of vertices
	 * @param edges the {@code n - 1} undirected tree edges as {@code [u, v]} pairs
	 * @return the centroid root labels, ascending
	 */
	int[] findMinHeightTrees(int n, int[][] edges) {
		// TODO: iteratively peel leaf layers until one or two centroid vertices remain.
		throw new UnsupportedOperationException("TODO: implement findMinHeightTrees");
	}
}
