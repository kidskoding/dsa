/** Applied — Problem 7: Flood fill an image from a start pixel. */
class Problem7 {

	/**
	 * Flood-fills {@code image} starting at {@code (sr, sc)}, recoloring every 4-directionally
	 * connected pixel that shares the start's original color.
	 *
	 * @param image the grid of pixel colors
	 * @param sr the start row
	 * @param sc the start column
	 * @param color the replacement color
	 * @return the modified image
	 */
	int[][] floodFill(int[][] image, int sr, int sc, int color) {
		// TODO: BFS/DFS from (sr, sc) over cells equal to the start color; guard no-op fills.
		throw new UnsupportedOperationException("TODO: implement floodFill");
	}
}
