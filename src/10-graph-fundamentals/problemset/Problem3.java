/** Foundational — Problem 3: BFS within k levels. */
class Problem3 {

	/**
	 * Returns, sorted ascending, every vertex within {@code k} BFS levels of {@code source}.
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the undirected edges as {@code [u, v]} pairs
	 * @param source the start vertex (distance 0)
	 * @param k the maximum BFS level to include
	 * @return the reachable vertices with distance {@code <= k}, ascending
	 */
	int[] reachableWithin(int vertexCount, int[][] edges, int source, int k) {
		// TODO: BFS from source, collecting vertices whose level is at most k.
		throw new UnsupportedOperationException("TODO: implement reachableWithin");
	}
}
