/** Applied — Problem 18: Course schedule ordering. */
class Problem18 {

	/**
	 * Returns a valid ordering of all courses, or an empty array if a cycle makes it impossible.
	 *
	 * @param numCourses the number of courses
	 * @param prerequisites pairs {@code [a, b]} meaning {@code b} must precede {@code a}
	 * @return a valid course order, or an empty array
	 */
	int[] findOrder(int numCourses, int[][] prerequisites) {
		// TODO: Kahn's algorithm; return empty when the queue drains with courses remaining.
		throw new UnsupportedOperationException("TODO: implement findOrder");
	}
}
