/** Applied — Problem 16: Course schedule feasibility. */
class Problem16 {

	/**
	 * Reports whether all courses can be finished, i.e. the prerequisite graph is acyclic.
	 *
	 * @param numCourses the number of courses
	 * @param prerequisites pairs {@code [a, b]} meaning {@code b} must precede {@code a}
	 * @return {@code true} if no cycle exists, otherwise {@code false}
	 */
	boolean canFinish(int numCourses, int[][] prerequisites) {
		// TODO: topological sort / cycle detection on the directed prerequisite graph.
		throw new UnsupportedOperationException("TODO: implement canFinish");
	}
}
