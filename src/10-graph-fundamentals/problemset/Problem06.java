import java.util.List;

/** Tier 1 — Problem 06: Decide bipartiteness and return the two color classes. */
public class Problem06 {

	/**
	 * Returns the two color classes of a bipartite undirected graph, or an empty list if the graph is
	 * not bipartite.
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the undirected edges as {@code [u, v]} pairs
	 * @return the two partition classes (each a list of vertices), or empty if not bipartite
	 */
	public List<List<Integer>> colorClasses(int vertexCount, int[][] edges) {
		// TODO: two-color the graph; on success bucket vertices by color, else return empty.
		throw new UnsupportedOperationException("TODO: implement colorClasses");
	}
}
