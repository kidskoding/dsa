/** Foundational — Problem 6: Bipartite check returning the color classes. */
class Problem6 {

	/**
	 * Returns a proper 2-coloring of an undirected graph, or an empty array if it is not
	 * bipartite. Isolated vertices default to color {@code 0}.
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the undirected edges as {@code [u, v]} pairs
	 * @return a {@code 0}/{@code 1} color per vertex, or an empty array
	 */
	int[] twoColoring(int vertexCount, int[][] edges) {
		// TODO: BFS/DFS color each component; report empty on a same-color edge.
		throw new UnsupportedOperationException("TODO: implement twoColoring");
	}
}
