import java.util.List;

/** Tier 1 — Problem 05: Topological order of a directed graph, or none if cyclic. */
public class Problem05 {

	/**
	 * Returns a valid topological ordering of the directed graph, or an empty list if the graph
	 * contains a cycle (no ordering exists).
	 *
	 * @param vertexCount the number of vertices
	 * @param edges the directed edges as {@code [u, v]} pairs meaning {@code u -> v}
	 * @return a topological order, or empty if the graph is cyclic
	 */
	public List<Integer> topologicalOrder(int vertexCount, int[][] edges) {
		// TODO: build the directed graph and run Kahn's algorithm.
		throw new UnsupportedOperationException("TODO: implement topologicalOrder");
	}
}
