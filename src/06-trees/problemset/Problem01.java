/**
 * Problem 01 — Height of a binary tree.
 *
 * <p>Given the root of a binary tree, compute its height: the number of edges on the longest
 * root-to-leaf path. An empty tree (null root) has height {@code -1} and a single node has height
 * {@code 0}.
 */
public class Problem01 {

	/**
	 * Returns the height of the tree rooted at {@code root} in edges.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the height in edges ({@code -1} for empty)
	 */
	public int height(TreeNode<Integer> root) {
		// TODO: recurse: empty => -1, otherwise 1 + max(height(left), height(right)).
		throw new UnsupportedOperationException("TODO: implement height");
	}
}
