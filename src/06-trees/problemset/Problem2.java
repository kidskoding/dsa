/**
 * Problem 2 — Count nodes.
 *
 * <p>Given the root of a binary tree, return the total number of nodes. An empty tree has
 * {@code 0} nodes.
 */
class Problem2 {

	/**
	 * Returns the number of nodes in the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the node count
	 */
	int count(TreeNode<Integer> root) {
		// TODO: empty => 0, else 1 + count(left) + count(right).
		throw new UnsupportedOperationException("TODO: implement count");
	}
}
