/**
 * Problem 11 — Diameter of a binary tree.
 *
 * <p>Given the root of a binary tree, return the length in edges of the longest path between any two
 * nodes; the path need not pass through the root.
 */
class Problem11 {

	/**
	 * Returns the diameter (longest path length in edges) of the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the diameter in edges
	 */
	int diameter(TreeNode<Integer> root) {
		// TODO: bottom-up; at each node the path through it is heightLeft + heightRight; track the max.
		throw new UnsupportedOperationException("TODO: implement diameter");
	}
}
