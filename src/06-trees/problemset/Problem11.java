/**
 * Problem 11 — Lowest common ancestor in a BST.
 *
 * <p>Given the root of a BST and two values both present in it, return their lowest common ancestor
 * using the ordering property in {@code O(h)} time.
 */
public class Problem11 {

	/**
	 * Returns the value of the lowest common ancestor of {@code a} and {@code b} in the BST rooted at
	 * {@code root}.
	 *
	 * @param root the root of the BST
	 * @param a a value present in the tree
	 * @param b a value present in the tree
	 * @return the LCA value
	 */
	public int lowestCommonAncestor(TreeNode<Integer> root, int a, int b) {
		// TODO: descend; the first node lying between a and b (inclusive) is the LCA.
		throw new UnsupportedOperationException("TODO: implement lowestCommonAncestor");
	}
}
