import java.util.List;

/**
 * Problem 21 — Zigzag level-order traversal.
 *
 * <p>Given the root of a binary tree, return its values in zigzag level order: level 0
 * left-to-right, level 1 right-to-left, alternating downward. An empty tree yields an empty list.
 */
class Problem21 {

	/**
	 * Returns the zigzag level-order traversal of the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the values grouped by level in zigzag order
	 */
	List<List<Integer>> zigzagLevelOrder(TreeNode<Integer> root) {
		// TODO: normal level-order, but reverse the collected values on odd levels.
		throw new UnsupportedOperationException("TODO: implement zigzagLevelOrder");
	}
}
