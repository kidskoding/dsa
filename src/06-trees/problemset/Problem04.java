/**
 * Problem 04 — Inorder successor in a BST.
 *
 * <p>Given the root of a BST and a target value present in the tree, return the next-larger value
 * (its inorder successor), or report that none exists.
 */
public class Problem04 {

	/**
	 * Returns the inorder successor of {@code target} in the BST rooted at {@code root}, or {@code
	 * null} when {@code target} is the maximum (no successor).
	 *
	 * @param root the root of the BST
	 * @param target a value present in the tree
	 * @return the successor value, or null if none
	 */
	public Integer inorderSuccessor(TreeNode<Integer> root, int target) {
		// TODO: descend tracking the last node where you turned left as a candidate successor.
		throw new UnsupportedOperationException("TODO: implement inorderSuccessor");
	}
}
