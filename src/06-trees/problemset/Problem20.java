import java.util.List;

/**
 * Problem 20 — Right side view.
 *
 * <p>Given the root of a binary tree, return the rightmost node value on each level, ordered top to
 * bottom. An empty tree yields an empty list.
 */
class Problem20 {

	/**
	 * Returns the right-side view of the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the rightmost value per level, top to bottom
	 */
	List<Integer> rightSideView(TreeNode<Integer> root) {
		// TODO: level-order traversal recording the last node of each level.
		throw new UnsupportedOperationException("TODO: implement rightSideView");
	}
}
