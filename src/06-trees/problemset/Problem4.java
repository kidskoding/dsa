import java.util.List;

/**
 * Problem 4 — Inorder traversal.
 *
 * <p>Given the root of a binary tree, return its node values in inorder: left subtree, then node,
 * then right subtree. For a BST this yields the values in sorted order.
 */
class Problem4 {

	/**
	 * Returns the inorder traversal of the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the values in inorder
	 */
	List<Integer> inorder(TreeNode<Integer> root) {
		// TODO: recurse left, visit node, recurse right, appending values in order.
		throw new UnsupportedOperationException("TODO: implement inorder");
	}
}
