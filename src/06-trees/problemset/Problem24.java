/**
 * Problem 24 — Subtree of another tree.
 *
 * <p>Given the roots of two binary trees {@code root} and {@code subRoot}, return whether some node
 * of {@code root} anchors a subtree structurally identical to {@code subRoot}.
 */
class Problem24 {

	/**
	 * Returns whether {@code subRoot} appears as a subtree of {@code root}.
	 *
	 * @param root the root of the larger tree, or null
	 * @param subRoot the root of the candidate subtree
	 * @return true if subRoot is a subtree of root
	 */
	boolean isSubtree(TreeNode<Integer> root, TreeNode<Integer> subRoot) {
		// TODO: at each node of root, test structural equality against subRoot.
		throw new UnsupportedOperationException("TODO: implement isSubtree");
	}
}
