import java.util.List;

/**
 * Problem 3 — Preorder traversal.
 *
 * <p>Given the root of a binary tree, return its node values in preorder: node, then left subtree,
 * then right subtree. An empty tree yields an empty list.
 */
class Problem3 {

	/**
	 * Returns the preorder traversal of the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the values in preorder
	 */
	List<Integer> preorder(TreeNode<Integer> root) {
		// TODO: visit node, then recurse left, then recurse right, appending values in order.
		throw new UnsupportedOperationException("TODO: implement preorder");
	}
}
