/**
 * Problem 8 — Invert a binary tree.
 *
 * <p>Given the root of a binary tree, swap the left and right child of every node and return the
 * new root, producing the mirror image of the original tree.
 */
class Problem8 {

	/**
	 * Inverts the tree rooted at {@code root} in place and returns its root.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the root of the inverted tree
	 */
	TreeNode<Integer> invert(TreeNode<Integer> root) {
		// TODO: recurse, swapping left and right at each node.
		throw new UnsupportedOperationException("TODO: implement invert");
	}
}
