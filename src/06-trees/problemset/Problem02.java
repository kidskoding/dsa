/**
 * Problem 02 — Count nodes.
 *
 * <p>Given the root of a binary tree, count the total number of nodes.
 */
public class Problem02 {

	/**
	 * Returns the number of nodes in the tree rooted at {@code root}.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the node count ({@code 0} for empty)
	 */
	public int countNodes(TreeNode<Integer> root) {
		// TODO: empty => 0, otherwise 1 + countNodes(left) + countNodes(right).
		throw new UnsupportedOperationException("TODO: implement countNodes");
	}
}
