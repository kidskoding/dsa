/**
 * Problem 03 — Validate a BST.
 *
 * <p>Given the root of a binary tree, decide whether it satisfies the binary-search-tree ordering
 * property: every node's value is greater than all values in its left subtree and less than all
 * values in its right subtree.
 */
public class Problem03 {

	/**
	 * Returns whether the tree rooted at {@code root} is a valid binary search tree.
	 *
	 * @param root the root node, or null for an empty tree (which is valid)
	 * @return true if the BST ordering property holds everywhere
	 */
	public boolean isValidBst(TreeNode<Integer> root) {
		// TODO: recurse carrying an open (lo, hi) bound; each value must fit strictly within.
		throw new UnsupportedOperationException("TODO: implement isValidBst");
	}
}
