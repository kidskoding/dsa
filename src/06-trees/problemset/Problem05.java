import java.util.List;

/**
 * Problem 05 — Level-order traversal.
 *
 * <p>Given the root of a binary tree, return its values grouped level by level from the root
 * downward.
 */
public class Problem05 {

	/**
	 * Returns the values of the tree rooted at {@code root} grouped by level (breadth-first).
	 *
	 * @param root the root node, or null for an empty tree
	 * @return a list of levels, each a list of values top-to-bottom, left-to-right
	 */
	public List<List<Integer>> levelOrder(TreeNode<Integer> root) {
		// TODO: BFS with a queue; pop a full level each iteration into its own list.
		throw new UnsupportedOperationException("TODO: implement levelOrder");
	}
}
