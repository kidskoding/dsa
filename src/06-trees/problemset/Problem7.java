/**
 * Problem 7 — Maximum depth of a binary tree.
 *
 * <p>Given the root of a binary tree, return its maximum depth: the number of nodes on the longest
 * root-to-leaf path. An empty tree has depth {@code 0}.
 */
class Problem7 {

	/**
	 * Returns the maximum depth of the tree rooted at {@code root}, in nodes.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the depth in nodes ({@code 0} for an empty tree)
	 */
	int maxDepth(TreeNode<Integer> root) {
		// TODO: empty => 0, else 1 + max(maxDepth(left), maxDepth(right)).
		throw new UnsupportedOperationException("TODO: implement maxDepth");
	}
}
