/**
 * Problem 10 — Symmetric tree.
 *
 * <p>Given the root of a binary tree, return whether it is a mirror of itself about its center: the
 * left subtree must be the mirror reflection of the right subtree.
 */
class Problem10 {

	/**
	 * Returns whether the tree rooted at {@code root} is symmetric about its center.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return true if the tree is symmetric
	 */
	boolean isSymmetric(TreeNode<Integer> root) {
		// TODO: compare left and right subtrees in mirrored order (left.left vs right.right, etc.).
		throw new UnsupportedOperationException("TODO: implement isSymmetric");
	}
}
