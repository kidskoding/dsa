import java.util.List;

/**
 * Problem 6 — Level-order traversal.
 *
 * <p>Given the root of a binary tree, return its values grouped level by level from the root
 * downward, left to right within each level. An empty tree yields an empty list.
 */
class Problem6 {

	/**
	 * Returns the level-order traversal of the tree rooted at {@code root}, one list per level.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return the values grouped by level
	 */
	List<List<Integer>> levelOrder(TreeNode<Integer> root) {
		// TODO: breadth-first with a queue, processing one full level per outer iteration.
		throw new UnsupportedOperationException("TODO: implement levelOrder");
	}
}
