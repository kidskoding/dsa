/**
 * Problem 9 — Same tree.
 *
 * <p>Given the roots of two binary trees, return whether they are structurally identical and every
 * corresponding pair of nodes holds the same value. Two empty trees are equal.
 */
class Problem9 {

	/**
	 * Returns whether the trees rooted at {@code p} and {@code q} are identical.
	 *
	 * @param p the root of the first tree, or null
	 * @param q the root of the second tree, or null
	 * @return true if the two trees are identical
	 */
	boolean isSameTree(TreeNode<Integer> p, TreeNode<Integer> q) {
		// TODO: both null => true; one null => false; else values equal and subtrees match.
		throw new UnsupportedOperationException("TODO: implement isSameTree");
	}
}
