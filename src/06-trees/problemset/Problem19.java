/**
 * Problem 19 — Kth smallest element in a BST.
 *
 * <p>Given the root of a binary search tree and an integer {@code k}, return the {@code k}-th
 * smallest value (1-indexed) without materializing the full sorted order.
 */
class Problem19 {

	/**
	 * Returns the {@code k}-th smallest value in the BST rooted at {@code root}.
	 *
	 * @param root the root of the BST (non-null)
	 * @param k the 1-indexed rank
	 * @return the k-th smallest value
	 */
	int kthSmallest(TreeNode<Integer> root, int k) {
		// TODO: inorder traversal, counting visited nodes; stop at the k-th.
		throw new UnsupportedOperationException("TODO: implement kthSmallest");
	}
}
