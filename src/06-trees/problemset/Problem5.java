/**
 * Problem 5 — Validate a BST.
 *
 * <p>Given the root of a binary tree, decide whether it is a valid binary search tree: every node's
 * value is strictly greater than all values in its left subtree and strictly less than all values in
 * its right subtree.
 */
class Problem5 {

	/**
	 * Returns whether the tree rooted at {@code root} satisfies the BST ordering property.
	 *
	 * @param root the root node, or null for an empty tree
	 * @return true if the tree is a valid BST
	 */
	boolean isValidBst(TreeNode<Integer> root) {
		// TODO: recurse with an open interval (lo, hi); each node's value must lie strictly inside.
		throw new UnsupportedOperationException("TODO: implement isValidBst");
	}
}
