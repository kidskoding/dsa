# Z-Algorithm

## The Z-Array Definition
*What does \\( Z[i] \\) measure about the prefix at each position?*

## The Z-Box Window
*How do the \\( [l, r] \\) bounds capture the current match segment?*

## Reusing Computed Values
*Why can previous Z-values be copied to skip redundant comparisons?*

## Linear-Time Argument \\( O(n) \\)
*Why does the right boundary only ever advance?*

## Pattern Matching via Concatenation
*How does \\( P\$T \\) turn the Z-array into a search?*

## Z-Array vs. Prefix Function
*How do the Z-array and KMP's \\( \pi \\) relate?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
