# Rabin–Karp

## Hashing a Window
*How is a fixed-length substring mapped to a numeric fingerprint?*

## Rolling Hash Update
*Why can the next window's hash be computed in \\( O(1) \\) from the previous?*

## Modular Arithmetic & Collisions
*How does the modulus control hash range, and what is a spurious hit?*

## Verifying Matches
*Why must a hash match be confirmed character-by-character?*

## Expected vs. Worst Case
*Why is it \\( O(n+m) \\) expected but \\( O(nm) \\) in the worst case?*

## Multi-Pattern & 2D Search
*How does fingerprinting extend to many patterns or grids?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
