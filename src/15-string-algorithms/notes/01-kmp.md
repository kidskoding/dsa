# Knuth–Morris–Pratt (KMP)

## Failure / LPS Function
*What does the longest-proper-prefix-suffix array encode for each position?*

## Building the Prefix Table
*How is the \\( \pi \\) array computed in \\( O(m) \\) over the pattern?*

## Avoiding Backtracking
*Why does the text pointer never move backward during matching?*

## Linear-Time Matching \\( O(n+m) \\)
*How does an amortized argument bound the total work?*

## Borders & Periodicity
*How do borders of the pattern relate to its repeating structure?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
