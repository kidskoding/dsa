/** Problem 20 — Carousel Decoder. See {@code PROBLEM_SET.md}. */
class Problem20 {

	/**
	 * Returns the number of rotation offsets {@code k} in {@code [0, n)} whose rotation of {@code s}
	 * (wrapping around) starts with {@code prefix}. If {@code prefix} is longer than {@code s}, the
	 * answer is 0.
	 *
	 * @param s the marquee string
	 * @param prefix the required rotation prefix
	 * @return the count of matching rotation offsets
	 */
	int countRotations(String s, String prefix) {
		// TODO: count matches of prefix in (s + s) at start indices k < n.
		throw new UnsupportedOperationException("TODO: implement countRotations");
	}
}
