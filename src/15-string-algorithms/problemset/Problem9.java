/** Problem 9 — Longest Happy Prefix. See {@code PROBLEM_SET.md}. */
class Problem9 {

	/**
	 * Returns the longest non-empty prefix of {@code s} that is also a proper suffix, using the
	 * prefix function. Returns the empty string when none exists.
	 *
	 * @param s the input string
	 * @return the longest happy prefix, or {@code ""}
	 */
	String longestPrefix(String s) {
		// TODO: build pi[] and return s.substring(0, pi[n-1]).
		throw new UnsupportedOperationException("TODO: implement longestPrefix");
	}
}
