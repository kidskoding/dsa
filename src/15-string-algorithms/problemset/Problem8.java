/** Problem 8 — Repeated Substring Pattern. See {@code PROBLEM_SET.md}. */
class Problem8 {

	/**
	 * Returns {@code true} if {@code s} can be built by repeating some proper substring (strictly
	 * shorter than {@code s}) two or more times.
	 *
	 * @param s the input string
	 * @return whether {@code s} is a repetition of a shorter block
	 */
	boolean repeatedSubstringPattern(String s) {
		// TODO: use n - pi[n-1] dividing n, or search s within (s + s).substring(1, 2n-1).
		throw new UnsupportedOperationException("TODO: implement repeatedSubstringPattern");
	}
}
