/** Problem 16 — Conveyor Tiling. See {@code PROBLEM_SET.md}. */
class Problem16 {

	/**
	 * Returns the length of the shortest block that, repeated a whole number of times, reproduces
	 * {@code s} exactly. When no proper tile divides the belt evenly, returns {@code s.length()}.
	 *
	 * @param s the belt string
	 * @return the shortest dividing tile length
	 */
	int tileLength(String s) {
		// TODO: p = n - pi[n-1]; return p if n % p == 0, else n.
		throw new UnsupportedOperationException("TODO: implement tileLength");
	}
}
