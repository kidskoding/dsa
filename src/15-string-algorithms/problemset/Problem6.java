/** Problem 6 — Implement strStr. See {@code PROBLEM_SET.md}. */
class Problem6 {

	/**
	 * Returns the index of the first occurrence of {@code needle} in {@code haystack}, or {@code -1}
	 * if absent. An empty {@code needle} matches at index 0.
	 *
	 * @param haystack the string to search
	 * @param needle the substring to find
	 * @return the 0-based first-match index, or {@code -1}
	 */
	int strStr(String haystack, String needle) {
		// TODO: scan with KMP (or brute force) and return the first match.
		throw new UnsupportedOperationException("TODO: implement strStr");
	}
}
