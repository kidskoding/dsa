/** Problem 5 — Count Occurrences. See {@code PROBLEM_SET.md}. */
class Problem5 {

	/**
	 * Returns the number of (possibly overlapping) occurrences of {@code pattern} in {@code text}
	 * using a linear-time matcher. An empty pattern occurs {@code text.length() + 1} times.
	 *
	 * @param text the text to search
	 * @param pattern the pattern to count
	 * @return the number of occurrences
	 */
	int countOccurrences(String text, String pattern) {
		// TODO: run KMP/Z over pattern + sep + text and count full matches.
		throw new UnsupportedOperationException("TODO: implement countOccurrences");
	}
}
