/** Problem 17 — Mirror Lock. See {@code PROBLEM_SET.md}. */
class Problem17 {

	/**
	 * Returns {@code true} if {@code s} is a palindrome or can become one by removing at most one
	 * character, using two pointers in O(n).
	 *
	 * @param s the code string
	 * @return whether the lock opens
	 */
	boolean opens(String s) {
		// TODO: two pointers; on mismatch test skipping either side once.
		throw new UnsupportedOperationException("TODO: implement opens");
	}
}
