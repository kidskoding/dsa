/** Tier 1 — Problem 02: Prefix Function. */
public class Problem02 {

	/**
	 * Computes the KMP prefix (failure) function of {@code pattern}. Entry {@code i} is the length of
	 * the longest proper prefix of {@code pattern[0..i]} that is also a suffix of it.
	 *
	 * @param pattern the pattern to analyze
	 * @return the prefix-function array
	 */
	public int[] prefixFunction(String pattern) {
		// TODO: build the failure function in O(m).
		throw new UnsupportedOperationException("TODO: implement prefixFunction");
	}
}
