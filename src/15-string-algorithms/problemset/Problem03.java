/** Tier 1 — Problem 03: Z-Array. */
public class Problem03 {

	/**
	 * Computes the Z-array of {@code s} in O(n). Entry {@code i} is the length of the longest common
	 * prefix of {@code s} and the suffix starting at {@code i}; entry {@code 0} is {@code 0}.
	 *
	 * @param s the string to analyze
	 * @return the Z-array
	 */
	public int[] zArray(String s) {
		// TODO: maintain the rightmost Z-box and reuse computed values.
		throw new UnsupportedOperationException("TODO: implement zArray");
	}
}
