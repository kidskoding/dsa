/** Problem 2 — Prefix Function. See {@code PROBLEM_SET.md}. */
class Problem2 {

	/**
	 * Computes the KMP prefix (failure) function of {@code s} in O(n): entry {@code i} is the length
	 * of the longest proper prefix of {@code s[0..i]} that is also a suffix of {@code s[0..i]}.
	 *
	 * @param s the input string
	 * @return the prefix-function array, length {@code s.length()}
	 */
	int[] prefixFunction(String s) {
		// TODO: extend the border length, falling back via pi[] on mismatch.
		throw new UnsupportedOperationException("TODO: implement prefixFunction");
	}
}
