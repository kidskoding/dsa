import java.util.List;

/** Tier 1 — Problem 01: Naive Matcher. */
public class Problem01 {

	/**
	 * Returns every start index where {@code pattern} occurs in {@code text}, in ascending order,
	 * using the brute-force O(nm) scan. Overlapping matches are reported; an empty pattern matches at
	 * every index {@code 0..text.length} inclusive.
	 *
	 * @param text the text to search
	 * @param pattern the pattern to find
	 * @return the list of 0-based match start indices
	 */
	public List<Integer> match(String text, String pattern) {
		// TODO: try every alignment and compare character-by-character.
		throw new UnsupportedOperationException("TODO: implement match");
	}
}
