/** Problem 18 — Border Patrol. See {@code PROBLEM_SET.md}. */
class Problem18 {

	/**
	 * Returns the longest non-empty proper prefix of {@code s} that is also a suffix (the longest
	 * border), as a substring, or the empty string when none exists. Read off the prefix function.
	 *
	 * @param s the fence string
	 * @return the longest seam, or {@code ""}
	 */
	String longestSeam(String s) {
		// TODO: build pi[] and return s.substring(0, pi[n-1]).
		throw new UnsupportedOperationException("TODO: implement longestSeam");
	}
}
