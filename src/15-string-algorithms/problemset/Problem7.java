/** Problem 7 — Valid Anagram. See {@code PROBLEM_SET.md}. */
class Problem7 {

	/**
	 * Returns {@code true} if {@code t} is an anagram of {@code s} (same multiset of characters),
	 * else {@code false}. Strings of different length are never anagrams.
	 *
	 * @param s the first string
	 * @param t the second string
	 * @return whether {@code t} is an anagram of {@code s}
	 */
	boolean isAnagram(String s, String t) {
		// TODO: compare character-frequency counts.
		throw new UnsupportedOperationException("TODO: implement isAnagram");
	}
}
