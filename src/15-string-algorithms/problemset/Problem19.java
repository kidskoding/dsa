/** Problem 19 — Pattern in Cyclic Rotations. See {@code PROBLEM_SET.md}. */
class Problem19 {

	/**
	 * Returns {@code true} if {@code pattern} occurs as a contiguous substring of some rotation of
	 * {@code text}, by matching it against {@code text + text} in linear time. The pattern is no
	 * longer than the text.
	 *
	 * @param text the rotating text
	 * @param pattern the pattern to find
	 * @return whether some rotation contains the pattern
	 */
	boolean occursInRotation(String text, String pattern) {
		// TODO: run a linear matcher for pattern inside (text + text).
		throw new UnsupportedOperationException("TODO: implement occursInRotation");
	}
}
