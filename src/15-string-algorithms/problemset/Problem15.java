/** Problem 15 — Shortest Period. See {@code PROBLEM_SET.md}. */
class Problem15 {

	/**
	 * Returns the length of the shortest period of {@code s}: the smallest {@code p} such that
	 * {@code s} is a prefix of an infinite repetition of {@code s[0..p)}. Equals {@code n - pi[n-1]}.
	 *
	 * @param s the input string
	 * @return the shortest period length
	 */
	int shortestPeriod(String s) {
		// TODO: build the prefix function and return n - pi[n-1].
		throw new UnsupportedOperationException("TODO: implement shortestPeriod");
	}
}
