/** Problem 3 — Z-Array. See {@code PROBLEM_SET.md}. */
class Problem3 {

	/**
	 * Computes the Z-array of {@code s} in O(n): entry {@code i} is the length of the longest
	 * substring starting at {@code i} that matches a prefix of {@code s}; {@code z[0]} is 0.
	 *
	 * @param s the input string
	 * @return the Z-array, length {@code s.length()}
	 */
	int[] zArray(String s) {
		// TODO: maintain the rightmost Z-box [l, r] and reuse it.
		throw new UnsupportedOperationException("TODO: implement zArray");
	}
}
