/**
 * Problem 6 — Sort Colors (Dutch National Flag). Given an array containing only {@code 0}, {@code 1},
 * and {@code 2}, sort it in place in a single pass with {@code O(1)} extra space using the three-way
 * partition. Return the same array after sorting.
 */
class Problem6 {

	/**
	 * Sorts {@code nums} (which contains only the values {@code 0}, {@code 1}, {@code 2}) in place so
	 * all zeros precede all ones precede all twos, and returns the same array reference.
	 *
	 * @param nums an array whose every element is {@code 0}, {@code 1}, or {@code 2}
	 * @return {@code nums}, sorted in non-decreasing order
	 */
	int[] sortColors(int[] nums) {
		// TODO: three-way (Dutch national flag) partition in one pass.
		throw new UnsupportedOperationException("TODO: implement sortColors");
	}
}
