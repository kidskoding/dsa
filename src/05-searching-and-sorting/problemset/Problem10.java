/**
 * Problem 10 — Search in Rotated Sorted Array. A sorted array of distinct integers is rotated at an
 * unknown pivot. Return the index of {@code target}, or {@code -1} if absent, in {@code O(log n)}
 * time without first locating the pivot separately.
 */
class Problem10 {

	/**
	 * Returns the index of {@code target} in {@code nums}, a once-ascending array of distinct values
	 * rotated at an unknown pivot, or {@code -1} if {@code target} is absent.
	 *
	 * @param nums a rotated ascending array of distinct integers
	 * @param target the value to locate
	 * @return the index of {@code target}, or {@code -1} if absent
	 */
	int search(int[] nums, int target) {
		// TODO: modified binary search that picks the sorted half each step.
		throw new UnsupportedOperationException("TODO: implement search");
	}
}
