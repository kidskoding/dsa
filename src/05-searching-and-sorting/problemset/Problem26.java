/**
 * Problem 26 — Maximum Gap. Return the maximum difference between successive elements in the sorted
 * form of {@code nums}, or {@code 0} if fewer than two elements. Run in linear time and space using
 * bucketing rather than a comparison sort.
 */
class Problem26 {

	/**
	 * Returns the largest gap between consecutive elements of {@code nums} once sorted, or {@code 0}
	 * if {@code nums} has fewer than two elements.
	 *
	 * @param nums the input array
	 * @return the maximum successive gap
	 */
	int maximumGap(int[] nums) {
		// TODO: bucket by value range (pigeonhole) and compare adjacent non-empty buckets.
		throw new UnsupportedOperationException("TODO: implement maximumGap");
	}
}
