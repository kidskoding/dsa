/**
 * Problem 05 — Merge two sorted arrays. Given two sorted arrays, return one merged sorted array. No
 * calls to a library sort.
 */
public class Problem05 {

	/**
	 * Returns a new array containing all elements of {@code a} and {@code b} in non-decreasing order,
	 * where both inputs are already sorted ascending.
	 *
	 * @param a the first sorted array
	 * @param b the second sorted array
	 * @return a freshly allocated merged, sorted array
	 */
	public int[] merge(int[] a, int[] b) {
		// TODO: walk both arrays with two indices, emitting the smaller front element each step.
		throw new UnsupportedOperationException("TODO: implement merge");
	}
}
