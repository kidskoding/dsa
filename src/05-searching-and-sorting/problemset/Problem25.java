/**
 * Problem 25 — Median of Two Sorted Arrays. Given two sorted arrays of sizes {@code m} and {@code n},
 * return the median of their union as a {@code double} in {@code O(log(min(m, n)))} time by binary-
 * searching a partition of the smaller array.
 */
class Problem25 {

	/**
	 * Returns the median of the combined sorted order of the two ascending arrays {@code a} and
	 * {@code b}.
	 *
	 * @param a an array sorted in ascending order
	 * @param b an array sorted in ascending order
	 * @return the median of the union of {@code a} and {@code b}
	 */
	double findMedianSortedArrays(int[] a, int[] b) {
		// TODO: binary-search a partition of the smaller array so the left halves form the lower half.
		throw new UnsupportedOperationException("TODO: implement findMedianSortedArrays");
	}
}
