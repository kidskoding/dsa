/**
 * Problem 9 — Find Peak Element. Return the index of any element strictly greater than its neighbors,
 * treating out-of-bounds neighbors as negative infinity. Run in {@code O(log n)} by walking toward
 * the higher neighbor.
 */
class Problem9 {

	/**
	 * Returns the index of any peak element of {@code nums} — one strictly greater than both of its
	 * in-bounds neighbors (the ends compare against negative infinity).
	 *
	 * @param nums an array with {@code nums[i] != nums[i + 1]} for all valid {@code i}
	 * @return the index of some peak element
	 */
	int findPeakElement(int[] nums) {
		// TODO: binary-search, stepping toward the larger neighbor.
		throw new UnsupportedOperationException("TODO: implement findPeakElement");
	}
}
