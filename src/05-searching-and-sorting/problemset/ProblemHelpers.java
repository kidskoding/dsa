/**
 * Small shared utilities reused across problem-set stubs (e.g. building or validating test fixtures
 * without leaning on a library sort). Implement these once; the individual {@code ProblemNN}
 * classes may call them.
 */
public final class ProblemHelpers {

	private ProblemHelpers() {}

	/**
	 * Returns {@code true} if {@code a} is in non-decreasing order.
	 *
	 * @param a the array to check (an empty or single-element array is trivially sorted)
	 * @return whether {@code a} is sorted ascending
	 */
	public static boolean isNonDecreasing(int[] a) {
		// TODO: verify a[i] <= a[i+1] for all adjacent pairs.
		throw new UnsupportedOperationException("TODO: implement isNonDecreasing");
	}
}
