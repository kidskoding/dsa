/**
 * Problem 8 — Single Element in a Sorted Array. Every value appears exactly twice except one. Return
 * the lone value in {@code O(log n)} time and {@code O(1)} space by binary-searching on index parity.
 */
class Problem8 {

	/**
	 * Returns the single non-duplicated value in {@code nums}, a sorted array in which every other
	 * value appears exactly twice.
	 *
	 * @param nums a non-decreasing array of odd length with exactly one unpaired value
	 * @return the unpaired value
	 */
	int singleNonDuplicate(int[] nums) {
		// TODO: binary-search on pair parity to find the unpaired value.
		throw new UnsupportedOperationException("TODO: implement singleNonDuplicate");
	}
}
