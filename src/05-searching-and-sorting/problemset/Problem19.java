/**
 * Problem 19 — Find the Duplicate Number. Given {@code n + 1} integers each in {@code [1, n]} with
 * exactly one repeated value, return that value without modifying the array and in better than
 * {@code O(n^2)} time (binary-search the value range counting elements {@code <= mid}).
 */
class Problem19 {

	/**
	 * Returns the single repeated value in {@code nums}, an array of {@code n + 1} integers each in
	 * the range {@code [1, n]}.
	 *
	 * @param nums the input array with exactly one repeated value
	 * @return the repeated value
	 */
	int findDuplicate(int[] nums) {
		// TODO: binary-search the value range by counting how many elements are <= mid.
		throw new UnsupportedOperationException("TODO: implement findDuplicate");
	}
}
