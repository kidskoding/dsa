/**
 * Problem 3 — Find First and Last Position. Given a non-decreasing array and a target, return the
 * {@code [first, last]} inclusive index range of the target's run, or {@code [-1, -1]} if absent.
 * Run in {@code O(log n)} time by binary-searching the two boundaries separately.
 */
class Problem3 {

	/**
	 * Returns a two-element array {@code [first, last]} giving the inclusive index range of
	 * {@code target} within the non-decreasing array {@code nums}, or {@code [-1, -1]} if absent.
	 *
	 * @param nums an array sorted in non-decreasing order
	 * @param target the value whose run to locate
	 * @return the {@code [first, last]} index pair, or {@code [-1, -1]} if absent
	 */
	int[] searchRange(int[] nums, int target) {
		// TODO: binary-search the leftmost and rightmost occurrences separately.
		throw new UnsupportedOperationException("TODO: implement searchRange");
	}
}
