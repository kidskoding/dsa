/**
 * Problem 24 — Split Array Largest Sum. Split {@code nums} into {@code k} non-empty contiguous
 * subarrays minimizing the largest subarray sum. Return that minimized largest sum by binary-
 * searching the answer over {@code [max(nums), sum(nums)]}.
 */
class Problem24 {

	/**
	 * Returns the minimized largest subarray sum when {@code nums} is split into {@code k} non-empty
	 * contiguous subarrays.
	 *
	 * @param nums the input array of non-negative integers
	 * @param k the number of contiguous parts, with {@code 1 <= k <= nums.length}
	 * @return the minimized largest part sum
	 */
	int splitArray(int[] nums, int k) {
		// TODO: binary-search the cap; greedily count parts needed for each candidate.
		throw new UnsupportedOperationException("TODO: implement splitArray");
	}
}
