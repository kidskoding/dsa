/**
 * Problem 17 — Sort Array by Increasing Frequency. Sort {@code nums} by ascending frequency of each
 * value, breaking ties by descending value. Return the rearranged array. Primary key is the count;
 * the tiebreak is the value reversed.
 */
class Problem17 {

	/**
	 * Returns {@code nums} reordered so values appear in increasing order of frequency, with ties
	 * broken by decreasing value.
	 *
	 * @param nums the input array
	 * @return the frequency-sorted array
	 */
	int[] frequencySort(int[] nums) {
		// TODO: count frequencies, then sort by (count asc, value desc) with a custom comparator.
		throw new UnsupportedOperationException("TODO: implement frequencySort");
	}
}
