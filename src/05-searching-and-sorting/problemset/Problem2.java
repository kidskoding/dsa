/**
 * Problem 2 — Search Insert Position. Given a sorted array of distinct integers and a target, return
 * the index of the target, or the index where it would be inserted to keep the array sorted. Run in
 * {@code O(log n)} time.
 */
class Problem2 {

	/**
	 * Returns the index of {@code target} in the ascending, distinct-valued array {@code nums}, or
	 * the index at which it would be inserted to preserve ascending order if it is absent.
	 *
	 * @param nums an array sorted in ascending order with distinct values
	 * @param target the value to locate or place
	 * @return the index of {@code target}, or its insertion point if absent
	 */
	int searchInsert(int[] nums, int target) {
		// TODO: binary-search for the leftmost index whose value is >= target.
		throw new UnsupportedOperationException("TODO: implement searchInsert");
	}
}
