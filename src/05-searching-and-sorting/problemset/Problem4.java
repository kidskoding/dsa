/**
 * Problem 4 — Merge Two Sorted Arrays. Given two non-decreasing arrays, return one merged
 * non-decreasing array containing all of their elements. Use a two-pointer merge in {@code O(m + n)}
 * time; do not call a library sort.
 */
class Problem4 {

	/**
	 * Returns a new array containing every element of {@code a} and {@code b} in non-decreasing
	 * order, merging the two sorted runs.
	 *
	 * @param a an array sorted in non-decreasing order
	 * @param b an array sorted in non-decreasing order
	 * @return the merged sorted array
	 */
	int[] merge(int[] a, int[] b) {
		// TODO: two-pointer merge of the two sorted runs.
		throw new UnsupportedOperationException("TODO: implement merge");
	}
}
