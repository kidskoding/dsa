/**
 * Problem 03 — Kth smallest element. Given an array and {@code k} (1-indexed), return the kth
 * smallest value. Aim for average linear time (quickselect-style).
 */
public class Problem03 {

	/**
	 * Returns the {@code k}-th smallest value in {@code a}, where {@code k} is 1-indexed (so {@code k
	 * == 1} is the minimum).
	 *
	 * @param a the input array
	 * @param k the 1-indexed rank to select, with {@code 1 <= k <= a.length}
	 * @return the kth smallest value
	 */
	public int kthSmallest(int[] a, int k) {
		// TODO: partition around a pivot and recurse only into the side containing rank k.
		throw new UnsupportedOperationException("TODO: implement kthSmallest");
	}
}
