/**
 * Problem 28 — External K-Way Merge. Merge {@code k} already-sorted integer streams into one
 * ascending array using a min-heap keyed on each stream's front, for {@code O(N log k)} total work.
 * Do not concatenate and re-sort.
 */
class Problem28 {

	/**
	 * Returns the merged ascending array of all elements across the sorted input {@code streams}.
	 *
	 * @param streams a list of arrays, each individually sorted ascending
	 * @return the fully merged ascending array
	 */
	int[] mergeKWay(int[][] streams) {
		// TODO: min-heap over the current front of each stream.
		throw new UnsupportedOperationException("TODO: implement mergeKWay");
	}
}
