/**
 * Problem 01 — Find first and last position. Given a sorted array and a target, return the {@code
 * (first, last)} index pair of the target's run, or {@code (-1, -1)} if absent. Use binary search;
 * do not scan linearly.
 */
public class Problem01 {

	/**
	 * Returns a two-element array {@code [first, last]} giving the inclusive index range of {@code
	 * target} within the ascending-sorted array {@code a}, or {@code [-1, -1]} if {@code target} is
	 * absent.
	 *
	 * @param a an array sorted in non-decreasing order
	 * @param target the value whose run to locate
	 * @return the {@code [first, last]} index pair, or {@code [-1, -1]} if not present
	 */
	public int[] searchRange(int[] a, int target) {
		// TODO: binary-search the leftmost and rightmost occurrences separately.
		throw new UnsupportedOperationException("TODO: implement searchRange");
	}
}
