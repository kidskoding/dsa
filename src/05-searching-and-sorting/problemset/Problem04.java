/**
 * Problem 04 — Sort a {0,1,2} array (Dutch national flag). Sort an array containing only the values
 * 0, 1, 2 in a single pass with {@code O(1)} extra space.
 */
public class Problem04 {

	/**
	 * Sorts {@code a} in place, where every element is 0, 1, or 2, using a single three-way partition
	 * pass.
	 *
	 * @param a the array to sort (sorted in place; elements are restricted to {@code {0, 1, 2}})
	 */
	public void sortColors(int[] a) {
		// TODO: maintain low/mid/high pointers and swap to grow the 0-region and 2-region.
		throw new UnsupportedOperationException("TODO: implement sortColors");
	}
}
