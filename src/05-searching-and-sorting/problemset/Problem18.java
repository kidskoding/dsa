/**
 * Problem 18 — Count Inversions. Count pairs {@code (i, j)} with {@code i < j} and
 * {@code nums[i] > nums[j]} in {@code O(n log n)} time by piggybacking on a merge sort. Return the
 * count as a {@code long}.
 */
class Problem18 {

	/**
	 * Returns the number of inversions in {@code nums}: pairs of indices {@code (i, j)} with
	 * {@code i < j} and {@code nums[i] > nums[j]}.
	 *
	 * @param nums the input array
	 * @return the inversion count
	 */
	long countInversions(int[] nums) {
		// TODO: merge sort, accumulating cross-pair inversions during each merge.
		throw new UnsupportedOperationException("TODO: implement countInversions");
	}
}
