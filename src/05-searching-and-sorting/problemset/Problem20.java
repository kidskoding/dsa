/**
 * Problem 20 — Merge Intervals. Merge all overlapping intervals (touching endpoints count as
 * overlapping) and return the non-overlapping intervals covering the input, sorted by start. Sort by
 * start first, then sweep.
 */
class Problem20 {

	/**
	 * Returns the merged set of {@code intervals}, with overlapping or touching intervals combined,
	 * sorted by start.
	 *
	 * @param intervals each {@code intervals[i]} is a two-element {@code [start, end]} pair
	 * @return the merged, non-overlapping intervals sorted by start
	 */
	int[][] merge(int[][] intervals) {
		// TODO: sort by start, then sweep and merge overlapping intervals.
		throw new UnsupportedOperationException("TODO: implement merge");
	}
}
