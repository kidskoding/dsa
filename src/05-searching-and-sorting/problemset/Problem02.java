/**
 * Problem 02 — Count inversions via merge sort. Return the number of pairs {@code (i, j)} with
 * {@code i < j} and {@code a[i] > a[j]}. Piggyback on the merge step.
 */
public class Problem02 {

	/**
	 * Returns the number of inversions in {@code a}, i.e. the count of index pairs {@code (i, j)}
	 * with {@code i < j} and {@code a[i] > a[j]}.
	 *
	 * @param a the input array (not required to be sorted; may be empty)
	 * @return the inversion count
	 */
	public long countInversions(int[] a) {
		// TODO: run merge sort, accumulating cross-inversions during each merge.
		throw new UnsupportedOperationException("TODO: implement countInversions");
	}
}
