/**
 * Problem 29 — Wiggle Sort. Reorder {@code nums} in place so that
 * {@code nums[0] < nums[1] > nums[2] < nums[3] ...} (strictly alternating); a valid arrangement is
 * guaranteed. Return the same array. Find the median via selection and interleave the halves.
 */
class Problem29 {

	/**
	 * Reorders {@code nums} in place into a strict wiggle pattern
	 * ({@code nums[0] < nums[1] > nums[2] < ...}) and returns the same array reference.
	 *
	 * @param nums the input array; a valid wiggle arrangement is guaranteed to exist
	 * @return {@code nums}, rearranged into a strict wiggle pattern
	 */
	int[] wiggleSort(int[] nums) {
		// TODO: find the median, then interleave lower and upper halves to avoid equal neighbors.
		throw new UnsupportedOperationException("TODO: implement wiggleSort");
	}
}
