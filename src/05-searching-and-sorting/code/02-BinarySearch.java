/**
 * Binary search over a <strong>sorted</strong> {@code int} array. Implement from scratch — see the
 * workbook page {@code binary-search.md}. Maintain a half-open or closed search window and avoid
 * the classic midpoint-overflow bug.
 */
class BinarySearch {

	/**
	 * Returns an index at which {@code target} occurs in the ascending-sorted array {@code a}, or
	 * {@code -1} if it is absent. When duplicates are present, any matching index is acceptable.
	 *
	 * @param a an array sorted in non-decreasing order
	 * @param target the value to find
	 * @return an index whose value equals {@code target}, or {@code -1} if not found
	 */
	public int search(int[] a, int target) {
		// TODO: maintain [lo, hi], compute mid without overflow, and shrink the window.
		throw new UnsupportedOperationException("TODO: implement binary search");
	}
}
