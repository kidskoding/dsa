/**
 * Selection sort: repeatedly select the minimum of the unsorted suffix and place it at the front.
 * An in-place \(O(n^2)\) sort. Implement from scratch — see {@code selection-sort.md}.
 */
class SelectionSort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using selection sort.
	 *
	 * @param a the array to sort (sorted in place; may be empty)
	 */
	public void sort(int[] a) {
		// TODO: for each prefix position, find the min of the suffix and swap it into place.
		throw new UnsupportedOperationException("TODO: implement selection sort");
	}
}
