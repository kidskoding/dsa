/**
 * Counting sort: a stable, non-comparison sort for small-range non-negative integer keys. Runs in
 * \(O(n + k)\) where {@code k} is the value range. Implement from scratch — see {@code
 * counting-sort.md}.
 */
class CountingSort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using counting sort. Assumes all values are
	 * non-negative.
	 *
	 * @param a the array to sort (sorted in place; may be empty; values {@code >= 0})
	 */
	public void sort(int[] a) {
		// TODO: tally counts per value, take a prefix sum, then place elements stably.
		throw new UnsupportedOperationException("TODO: implement counting sort");
	}
}
