/**
 * Merge sort: divide the array in half, sort each half recursively, and merge. A stable \(O(n\log
 * n)\) sort. Implement from scratch — see {@code merge-sort.md}.
 */
class MergeSort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using merge sort.
	 *
	 * @param a the array to sort (sorted in place; may be empty)
	 */
	public void sort(int[] a) {
		// TODO: recursively sort halves, then merge them with a linear merge step.
		throw new UnsupportedOperationException("TODO: implement merge sort");
	}
}
