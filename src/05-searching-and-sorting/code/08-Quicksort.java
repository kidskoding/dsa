/**
 * Quicksort: partition around a pivot and recurse on each side. An in-place sort with \(O(n\log
 * n)\) expected time and \(O(n^2)\) worst case. Implement from scratch — see {@code quicksort.md}.
 */
class Quicksort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using quicksort.
	 *
	 * @param a the array to sort (sorted in place; may be empty)
	 */
	public void sort(int[] a) {
		// TODO: choose a pivot, partition in place, and recurse on the two sides.
		throw new UnsupportedOperationException("TODO: implement quicksort");
	}
}
