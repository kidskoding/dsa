/**
 * Linear (sequential) search over an unsorted {@code int} array. Implement from scratch — see the
 * workbook page {@code linear-search.md}.
 */
class LinearSearch {

	/**
	 * Returns the index of the first occurrence of {@code target} in {@code a}, scanning left to
	 * right, or {@code -1} if it is absent.
	 *
	 * @param a the array to scan (may be empty)
	 * @param target the value to find
	 * @return index of the first match, or {@code -1} if not found
	 */
	public int search(int[] a, int target) {
		// TODO: scan a[0..n-1] and return the first index whose value equals target.
		throw new UnsupportedOperationException("TODO: implement linear search");
	}
}
