/**
 * Bucket sort: distribute elements into buckets by value range, sort each bucket, and concatenate.
 * Runs in \(O(n)\) expected time on uniformly distributed input. Implement from scratch — see
 * {@code bucket-sort.md}.
 */
class BucketSort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using bucket sort.
	 *
	 * @param a the array to sort (sorted in place; may be empty)
	 */
	public void sort(int[] a) {
		// TODO: scatter into buckets by value range, sort each bucket, then gather in order.
		throw new UnsupportedOperationException("TODO: implement bucket sort");
	}
}
