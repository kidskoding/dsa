/**
 * Heapsort: build a binary max-heap in place, then repeatedly extract the max to the end. An
 * in-place, non-stable \(O(n\log n)\) sort. Implement from scratch — see {@code heapsort.md}.
 */
class Heapsort {

	/**
	 * Sorts {@code a} in place into non-decreasing order using heapsort.
	 *
	 * @param a the array to sort (sorted in place; may be empty)
	 */
	public void sort(int[] a) {
		// TODO: build a max-heap with bottom-up sift-down, then extract-max into the suffix.
		throw new UnsupportedOperationException("TODO: implement heapsort");
	}
}
