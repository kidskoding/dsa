# Activity Selection

## Problem Setup
*What are the intervals, and what makes two activities compatible?*

## Earliest-Finish-Time Rule
*Why pick the activity that finishes first among the feasible ones?*

## Correctness via Exchange
*How does swapping show the earliest-finishing choice is safe?*

## Sorting & \\( O(n \log n) \\)
*Where does the sort dominate, and when is the scan \\( O(n) \\)?*

## Counterexamples to Other Rules
*Why do "shortest duration" or "fewest conflicts" heuristics fail?*

## Weighted Variant
*Why does adding weights break greedy and force DP?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
