/** Greedy Algorithms — Problem 6: Minimum Platforms. */
class Problem06 {

	/**
	 * Returns the minimum number of platforms so no train waits, where a departure must be strictly
	 * before the next arrival. Sort both arrays and sweep tracking concurrent occupancy.
	 *
	 * @param arrivals the train arrival times
	 * @param departures the train departure times, parallel to {@code arrivals}
	 * @return the minimum number of platforms needed
	 */
	int minPlatforms(int[] arrivals, int[] departures) {
		// TODO: sort both; two-pointer sweep counting peak concurrent trains.
		throw new UnsupportedOperationException("TODO: implement minPlatforms");
	}
}
