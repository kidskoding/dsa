/** Greedy Algorithms — Problem 24: Job Scheduling for Maximum Profit. */
class Problem24 {

	/**
	 * Returns the maximum total reward from scheduling unit-time jobs, where each chosen job must
	 * run by its {@code deadline}. Sort by reward descending and place each in the latest free slot.
	 *
	 * @param deadlines the job deadlines (1-indexed slots)
	 * @param rewards the job rewards, parallel to {@code deadlines}
	 * @return the maximum total reward
	 */
	long maxReward(int[] deadlines, int[] rewards) {
		// TODO: sort by reward descending; assign each job to the latest free slot <= deadline.
		throw new UnsupportedOperationException("TODO: implement maxReward");
	}
}
