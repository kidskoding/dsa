/** Greedy Algorithms — Problem 19: Candy. */
class Problem19 {

	/**
	 * Returns the minimum total candies so each child gets at least one and any child with a
	 * strictly higher rating than a neighbor gets more. Sweep left then right, taking the max.
	 *
	 * @param ratings the children's ratings in line order
	 * @return the minimum total candies
	 */
	int candy(int[] ratings) {
		// TODO: left-to-right pass, then right-to-left pass taking the maximum.
		throw new UnsupportedOperationException("TODO: implement candy");
	}
}
