/** Greedy Algorithms — Problem 1: Minimum Coins (Canonical System). */
class Problem01 {

	/**
	 * Returns the minimum number of coins whose values sum to {@code amount}, taking the largest
	 * coin first. The system is canonical and ascending with {@code coins[0] == 1}.
	 *
	 * @param coins the ascending coin denominations, including {@code 1}
	 * @param amount the non-negative target amount
	 * @return the minimum coin count, or {@code 0} when {@code amount} is {@code 0}
	 */
	int minCoins(int[] coins, int amount) {
		// TODO: scan denominations high to low, taking amount / coin of each.
		throw new UnsupportedOperationException("TODO: implement minCoins");
	}
}
