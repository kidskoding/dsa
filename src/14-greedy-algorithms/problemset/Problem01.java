/** Tier 1 — Problem 01: Minimum Coins (Canonical System). */
public class Problem01 {

	/**
	 * Returns the minimum number of coins whose values sum to {@code amount}, assuming {@code coins}
	 * is a canonical system so the largest-coin-first greedy is optimal. Returns {@code 0} when
	 * {@code amount == 0}.
	 *
	 * @param coins the available denominations (each positive)
	 * @param amount the non-negative target amount
	 * @return the minimum number of coins summing to {@code amount}
	 */
	public int minCoins(int[] coins, int amount) {
		// TODO: take as many of the largest coin as fit, then recurse on the remainder.
		throw new UnsupportedOperationException("TODO: implement minCoins");
	}
}
