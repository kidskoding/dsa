/** Greedy Algorithms — Problem 10: Maximum Units on a Truck. */
class Problem10 {

	/**
	 * Returns the maximum total units that fit on a truck holding {@code truckSize} boxes, where
	 * {@code boxTypes[i] = [count, unitsPerBox]}. Load highest-unit boxes first.
	 *
	 * @param boxTypes the box types as {@code [count, unitsPerBox]} pairs
	 * @param truckSize the maximum number of boxes
	 * @return the maximum total units carried
	 */
	int maximumUnits(int[][] boxTypes, int truckSize) {
		// TODO: sort by unitsPerBox descending; fill until truck is full.
		throw new UnsupportedOperationException("TODO: implement maximumUnits");
	}
}
