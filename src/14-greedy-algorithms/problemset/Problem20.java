/** Greedy Algorithms — Problem 20: Task Scheduler. */
class Problem20 {

	/**
	 * Returns the minimum number of intervals (busy or idle) to finish all {@code tasks}, where the
	 * same task must be separated by at least {@code n} cooldown intervals.
	 *
	 * @param tasks the CPU tasks as uppercase letters
	 * @param n the required cooldown between identical tasks
	 * @return the minimum number of intervals
	 */
	int leastInterval(char[] tasks, int n) {
		// TODO: bound by the most frequent task's forced gaps, else total length.
		throw new UnsupportedOperationException("TODO: implement leastInterval");
	}
}
