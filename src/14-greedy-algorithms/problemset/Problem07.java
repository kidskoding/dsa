/** Greedy Algorithms — Problem 7: Jump Game. */
class Problem07 {

	/**
	 * Returns whether the last index is reachable from index {@code 0}, where {@code nums[i]} is the
	 * maximum forward jump from {@code i}. Track the farthest reachable index in one scan.
	 *
	 * @param nums the non-negative maximum jump lengths
	 * @return {@code true} if the last index is reachable
	 */
	boolean canJump(int[] nums) {
		// TODO: track farthest reach; fail if any index is beyond it.
		throw new UnsupportedOperationException("TODO: implement canJump");
	}
}
