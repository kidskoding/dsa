/** Greedy Algorithms — Problem 15: Can Place Flowers. */
class Problem15 {

	/**
	 * Returns whether {@code n} new flowers can be planted in {@code flowerbed} without placing two
	 * adjacent flowers. Greedily plant in each empty slot whose neighbors are both empty.
	 *
	 * @param flowerbed the bed of {@code 0} (empty) and {@code 1} (planted) slots
	 * @param n the number of flowers to plant
	 * @return {@code true} if all {@code n} flowers can be planted
	 */
	boolean canPlaceFlowers(int[] flowerbed, int n) {
		// TODO: scan slots; plant where both neighbors are empty, counting placements.
		throw new UnsupportedOperationException("TODO: implement canPlaceFlowers");
	}
}
