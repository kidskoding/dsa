import java.util.List;

/** Greedy Algorithms — Problem 11: Partition Labels. */
class Problem11 {

	/**
	 * Partitions {@code s} into the most contiguous parts so each letter appears in one part, and
	 * returns the part sizes in order. Extend each part to the farthest last-occurrence seen.
	 *
	 * @param s the lowercase string to partition
	 * @return the list of part sizes in order
	 */
	List<Integer> partitionLabels(String s) {
		// TODO: precompute last occurrences; close a part when the scan reaches its end.
		throw new UnsupportedOperationException("TODO: implement partitionLabels");
	}
}
