/** Greedy Algorithms — Problem 21: Queue Reconstruction by Height. */
class Problem21 {

	/**
	 * Reconstructs the queue from {@code [height, k]} entries, where {@code k} is the number of
	 * people at least as tall in front. Sort by height descending, k ascending; insert at index k.
	 *
	 * @param people the entries as {@code [height, k]} pairs
	 * @return the reconstructed queue
	 */
	int[][] reconstructQueue(int[][] people) {
		// TODO: sort tallest-first (k ascending); insert each person at index k.
		throw new UnsupportedOperationException("TODO: implement reconstructQueue");
	}
}
