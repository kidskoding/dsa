/** Greedy Algorithms — Problem 17: Two City Scheduling. */
class Problem17 {

	/**
	 * Returns the minimum total cost to send {@code n} people to city A and {@code n} to city B,
	 * where {@code costs[i] = [aCost, bCost]}. Sort by {@code aCost - bCost}.
	 *
	 * @param costs the per-person {@code [aCost, bCost]} pairs ({@code 2n} entries)
	 * @return the minimum total cost
	 */
	int twoCitySchedCost(int[][] costs) {
		// TODO: sort by aCost - bCost; send the first n to A and the rest to B.
		throw new UnsupportedOperationException("TODO: implement twoCitySchedCost");
	}
}
