/** Greedy Algorithms — Problem 8: Assign Cookies. */
class Problem08 {

	/**
	 * Returns the maximum number of content children, where cookie {@code s[j]} contents child
	 * {@code i} if {@code s[j] >= g[i]}. Sort both and match with two pointers.
	 *
	 * @param g the children's greed factors
	 * @param s the cookie sizes
	 * @return the maximum number of content children
	 */
	int findContentChildren(int[] g, int[] s) {
		// TODO: sort both; advance pointers to greedily satisfy children.
		throw new UnsupportedOperationException("TODO: implement findContentChildren");
	}
}
