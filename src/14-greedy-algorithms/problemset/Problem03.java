/** Greedy Algorithms — Problem 3: Maximum Non-Overlapping Intervals. */
class Problem03 {

	/**
	 * Returns the maximum number of mutually non-overlapping intervals (activity selection). Sort
	 * by end time and greedily accept intervals whose start is at least the last accepted end.
	 *
	 * @param intervals the intervals as {@code [start, end)} pairs
	 * @return the maximum count of non-overlapping intervals
	 */
	int maxNonOverlapping(int[][] intervals) {
		// TODO: sort by end; greedily accept compatible intervals.
		throw new UnsupportedOperationException("TODO: implement maxNonOverlapping");
	}
}
