/** Tier 2 — Problem 9: Points within a radius. */
public class Problem9 {

	/**
	 * Returns how many points in {@code points} lie within (inclusive) Euclidean distance
	 * {@code radius} of {@code center = [cx, cy]}. Compare squared distances against
	 * {@code radius * radius}.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param center the center {@code [cx, cy]}
	 * @param radius the inclusive radius
	 * @return the number of points within the radius
	 */
	public int countWithinRadius(int[][] points, int[] center, int radius) {
		// TODO: count points whose squared distance is at most radius*radius.
		throw new UnsupportedOperationException("TODO: implement countWithinRadius");
	}
}
