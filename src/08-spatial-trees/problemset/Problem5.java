import java.util.List;

/** Tier 1 — Problem 5: Brute-force range query. */
public class Problem5 {

	/**
	 * Returns the indices of all points in {@code points} that fall inside the axis-aligned rectangle
	 * {@code rect = [xMin, yMin, xMax, yMax]} (boundary included), in increasing index order.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param rect the inclusive query bounds {@code [xMin, yMin, xMax, yMax]}
	 * @return the indices of contained points, ascending
	 */
	public List<Integer> rangeQuery(int[][] points, int[] rect) {
		// TODO: scan every point and collect indices inside the rectangle.
		throw new UnsupportedOperationException("TODO: implement rangeQuery");
	}
}
