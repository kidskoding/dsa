/** Tier 1 — Problem 1: Squared Euclidean distance. */
public class Problem1 {

	/**
	 * Returns the squared Euclidean distance between {@code a = [ax, ay]} and {@code b = [bx, by]},
	 * computed as {@code (ax-bx)^2 + (ay-by)^2}. The square root is omitted so comparisons stay exact.
	 *
	 * @param a the first point as {@code [x, y]}
	 * @param b the second point as {@code [x, y]}
	 * @return the squared distance between {@code a} and {@code b}
	 */
	public long squaredDistance(int[] a, int[] b) {
		// TODO: return dx*dx + dy*dy using long arithmetic.
		throw new UnsupportedOperationException("TODO: implement squaredDistance");
	}
}
