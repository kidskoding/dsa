/** Tier 2 — Problem 15: Maximum points in a unit-side square. */
public class Problem15 {

	/**
	 * Returns the maximum number of points an axis-aligned {@code s}-by-{@code s} square can cover when
	 * placed anywhere, counting points on the boundary.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param s the side length of the square
	 * @return the most points coverable by one square
	 */
	public int maxPointsInSquare(int[][] points, int s) {
		// TODO: try square positions anchored at point coordinates and count coverage.
		throw new UnsupportedOperationException("TODO: implement maxPointsInSquare");
	}
}
