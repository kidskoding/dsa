/** Tier 2 — Problem 18: Count pairs within distance. */
public class Problem18 {

	/**
	 * Returns the number of unordered pairs of distinct points in {@code points} whose Euclidean
	 * distance is at most {@code d}. Compare squared distances against {@code d * d}.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param d the inclusive distance threshold
	 * @return the count of close pairs
	 */
	public long countPairsWithinDistance(int[][] points, int d) {
		// TODO: count pairs whose squared distance is at most d*d.
		throw new UnsupportedOperationException("TODO: implement countPairsWithinDistance");
	}
}
