/** Tier 2 — Problem 6: K closest points to origin. */
public class Problem6 {

	/**
	 * Returns the {@code k} points closest to the origin {@code (0, 0)} by Euclidean distance, each as
	 * {@code [x, y]}. Compare by squared distance; the result may be in any order. Assumes
	 * {@code 0 <= k <= points.length}.
	 *
	 * @param points the candidate points, each as {@code [x, y]}
	 * @param k how many of the closest points to return
	 * @return the {@code k} closest points, in any order
	 */
	public int[][] kClosest(int[][] points, int k) {
		// TODO: sort or select by x*x + y*y and take the k nearest.
		throw new UnsupportedOperationException("TODO: implement kClosest");
	}
}
