/** Tier 1 — Problem 02: Point inside a rectangle. */
public class Problem02 {

	/**
	 * Returns whether {@code p} lies inside the inclusive axis-aligned rectangle {@code [xMin, xMax]
	 * x [yMin, yMax]}.
	 *
	 * @param p the point to test
	 * @param xMin the inclusive lower x bound
	 * @param yMin the inclusive lower y bound
	 * @param xMax the inclusive upper x bound
	 * @param yMax the inclusive upper y bound
	 * @return {@code true} if {@code p} is within the rectangle
	 */
	public boolean contains(Point2D p, double xMin, double yMin, double xMax, double yMax) {
		// TODO: check x and y against the inclusive bounds.
		throw new UnsupportedOperationException("TODO: implement contains");
	}
}
