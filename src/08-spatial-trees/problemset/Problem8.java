/** Tier 2 — Problem 8: Nearest neighbour among points. */
public class Problem8 {

	/**
	 * Returns the index of the point in {@code points} closest to {@code query = [qx, qy]} by Euclidean
	 * distance, breaking ties by smallest index. Assumes {@code points} is non-empty.
	 *
	 * @param points the candidate points, each as {@code [x, y]}
	 * @param query the query point {@code [qx, qy]}
	 * @return the index of the nearest point
	 */
	public int nearest(int[][] points, int[] query) {
		// TODO: track the minimum squared distance and its index.
		throw new UnsupportedOperationException("TODO: implement nearest");
	}
}
