/** Tier 1 — Problem 4: Quadrant of a point. */
public class Problem4 {

	/**
	 * Returns the quadrant of {@code point = [px, py]} relative to {@code center = [cx, cy]}, as one
	 * of {@code "NE"}, {@code "NW"}, {@code "SE"}, {@code "SW"}. A point with {@code px >= cx} is East
	 * and {@code py >= cy} is North.
	 *
	 * @param center the splitting center {@code [cx, cy]}
	 * @param point the query point {@code [px, py]}
	 * @return the quadrant name
	 */
	public String quadrant(int[] center, int[] point) {
		// TODO: combine the East/West and North/South tests into a quadrant string.
		throw new UnsupportedOperationException("TODO: implement quadrant");
	}
}
