/** Tier 1 — Problem 01: Squared Euclidean distance. */
public class Problem01 {

	/**
	 * Returns the squared Euclidean distance between {@code a} and {@code b}, avoiding the square
	 * root so the result stays exact for nearest-neighbor comparisons.
	 *
	 * @param a the first point
	 * @param b the second point
	 * @return the squared distance between {@code a} and {@code b}
	 */
	public double squaredDistance(Point2D a, Point2D b) {
		// TODO: return dx*dx + dy*dy.
		throw new UnsupportedOperationException("TODO: implement squaredDistance");
	}
}
