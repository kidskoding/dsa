/** Tier 2 — Problem 7: Count points in a rectangle. */
public class Problem7 {

	/**
	 * Returns how many points in {@code points} lie inside the axis-aligned rectangle
	 * {@code rect = [xMin, yMin, xMax, yMax]}, with the boundary counting as inside.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param rect the inclusive query bounds {@code [xMin, yMin, xMax, yMax]}
	 * @return the number of points inside the rectangle
	 */
	public int countInRectangle(int[][] points, int[] rect) {
		// TODO: scan and count points within the inclusive bounds.
		throw new UnsupportedOperationException("TODO: implement countInRectangle");
	}
}
