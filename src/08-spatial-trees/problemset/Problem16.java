/** Tier 2 — Problem 16: Closest pair (sorted sweep). */
public class Problem16 {

	/**
	 * Returns the smallest squared Euclidean distance between any two distinct points in
	 * {@code points}, using a sorted sweep with a strip rather than the naive all-pairs scan. The
	 * result equals the brute-force answer. Assumes {@code points.length >= 2}.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @return the minimum squared distance over all pairs
	 */
	public long closestPair(int[][] points) {
		// TODO: sort by x, sweep maintaining a strip of width equal to the current best distance.
		throw new UnsupportedOperationException("TODO: implement closestPair");
	}
}
