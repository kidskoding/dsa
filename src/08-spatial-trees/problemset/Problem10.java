/** Tier 2 — Problem 10: Quadrant counts. */
public class Problem10 {

	/**
	 * Counts how many points fall in each quadrant relative to {@code center = [cx, cy]} and returns
	 * {@code [ne, nw, sw, se]}. A point with {@code px >= cx} is East and {@code py >= cy} is North.
	 *
	 * @param points the points, each as {@code [x, y]}
	 * @param center the splitting center {@code [cx, cy]}
	 * @return counts {@code [ne, nw, sw, se]}
	 */
	public int[] quadrantCounts(int[][] points, int[] center) {
		// TODO: classify each point and tally the four quadrants.
		throw new UnsupportedOperationException("TODO: implement quadrantCounts");
	}
}
