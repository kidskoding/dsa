/** Tier 1 — Problem 2: Point inside a rectangle. */
public class Problem2 {

	/**
	 * Returns whether {@code point = [px, py]} lies inside the axis-aligned rectangle
	 * {@code rect = [xMin, yMin, xMax, yMax]}, with the boundary counting as inside.
	 *
	 * @param rect the rectangle's inclusive bounds {@code [xMin, yMin, xMax, yMax]}
	 * @param point the query point {@code [x, y]}
	 * @return {@code true} if the point is inside or on the rectangle
	 */
	public boolean contains(int[] rect, int[] point) {
		// TODO: check xMin <= px <= xMax and yMin <= py <= yMax.
		throw new UnsupportedOperationException("TODO: implement contains");
	}
}
