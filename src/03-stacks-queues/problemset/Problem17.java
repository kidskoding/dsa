/**
 * Problem 17 — Validate Stack Sequences.
 *
 * <p>Return whether {@code popped} could result from pushing and popping {@code pushed} on an
 * initially empty stack. Simulate with a stack.
 */
final class Problem17 {

	private Problem17() {}

	/**
	 * Returns whether the push/pop sequences are consistent.
	 *
	 * @param pushed the push order
	 * @param popped the claimed pop order
	 * @return {@code true} if the sequences are achievable
	 */
	static boolean validateStackSequences(int[] pushed, int[] popped) {
		// TODO: push values; pop whenever the top matches the next popped value.
		throw new UnsupportedOperationException("TODO: implement validateStackSequences");
	}
}
