/**
 * Problem 6 — Sliding Window Maximum.
 *
 * <p>Return the maximum of each contiguous window of size {@code k} using a monotonic deque in
 * {@code O(n)} time.
 */
final class Problem6 {

	private Problem6() {}

	/**
	 * Returns the maximum value in each sliding window of width {@code k}.
	 *
	 * @param nums the input array
	 * @param k the window size
	 * @return an array of window maximums
	 */
	static int[] maxSlidingWindow(int[] nums, int k) {
		// TODO: maintain a deque of indices in decreasing value order.
		throw new UnsupportedOperationException("TODO: implement maxSlidingWindow");
	}
}
