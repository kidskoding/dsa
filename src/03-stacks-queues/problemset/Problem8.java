/**
 * Problem 8 — Backspace String Compare.
 *
 * <p>The character {@code '#'} acts as a backspace. Return whether two strings are equal after
 * applying all backspaces, building each result with a stack.
 */
final class Problem8 {

	private Problem8() {}

	/**
	 * Returns whether the two strings are equal after applying backspaces.
	 *
	 * @param s the first string
	 * @param t the second string
	 * @return {@code true} if the typed results are equal
	 */
	static boolean backspaceCompare(String s, String t) {
		// TODO: build each result with a stack and compare.
		throw new UnsupportedOperationException("TODO: implement backspaceCompare");
	}
}
