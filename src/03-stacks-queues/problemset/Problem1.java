/**
 * Problem 1 — Valid Parentheses.
 *
 * <p>Given a string of brackets drawn from {@code ()[]{}}, return whether every opening bracket is
 * closed by a matching closing bracket in the correct order. Solve it in {@code O(n)} time.
 */
final class Problem1 {

	private Problem1() {}

	/**
	 * Returns whether {@code s} is a valid, balanced bracket string.
	 *
	 * @param s a string consisting only of the characters {@code ()[]{}}
	 * @return {@code true} if every bracket is correctly matched and nested
	 */
	static boolean isValid(String s) {
		// TODO: push opening brackets; on a closing bracket the top must be its match.
		throw new UnsupportedOperationException("TODO: implement isValid");
	}
}
