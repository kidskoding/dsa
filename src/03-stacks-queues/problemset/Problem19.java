/**
 * Problem 19 — Remove K Digits.
 *
 * <p>Remove exactly {@code k} digits from {@code num} to form the smallest possible number, with
 * leading zeros stripped. Use a monotonic increasing stack of digits.
 */
final class Problem19 {

	private Problem19() {}

	/**
	 * Returns the smallest number formed by removing {@code k} digits.
	 *
	 * @param num the input number as a string
	 * @param k the number of digits to remove
	 * @return the smallest resulting number, or {@code "0"} if empty
	 */
	static String removeKdigits(String num, int k) {
		// TODO: pop larger digits while removals remain; strip leading zeros at the end.
		throw new UnsupportedOperationException("TODO: implement removeKdigits");
	}
}
