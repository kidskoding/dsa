/**
 * Problem 09: Path With Minimum Effort (bottleneck Dijkstra). Minimize the maximum adjacent height
 * difference on a route from the top-left to the bottom-right cell.
 */
class Problem09 {

	/**
	 * Returns the minimum effort to travel from the top-left to the bottom-right cell.
	 *
	 * @param heights the grid of cell heights
	 * @return the minimum possible maximum adjacent height difference
	 */
	int minimumEffortPath(int[][] heights) {
		// TODO: Dijkstra where a path's cost is the max edge weight (height difference) rather than the sum.
		throw new UnsupportedOperationException("TODO: implement");
	}
}
