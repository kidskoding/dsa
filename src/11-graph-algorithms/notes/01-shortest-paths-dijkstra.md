# Dijkstra's Shortest Paths

## Greedy Relaxation
*What invariant does the greedy choice rely on, and why is the popped vertex final?*

## Priority Queue & Lazy Deletion
*How does the heap drive the algorithm, and what do you do with stale entries?*

## Why It Fails on Negative Edges
*Sketch a tiny graph where the greedy assumption breaks. What goes wrong?*

## Relaxation Predecessor & Path Reconstruction
*How do you recover the actual path, not just the distance?*

## Complexity \\( O((V+E)\log V) \\)
*Where does each \\( \log V \\) factor come from? Compare binary heap vs Fibonacci heap.*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
