# Edmonds-Karp Maximum Flow

## BFS as the Augmenting-Path Rule
*Why pick the shortest (fewest-edge) augmenting path each iteration?*

## Shortest Augmenting Paths
*How does BFS guarantee monotonically non-decreasing path lengths?*

## Bounding the Number of Augmentations
*Why is each edge saturated as a bottleneck only \\( O(V) \\) times?*

## Complexity \\( O(VE^2) \\)
*Combine the augmentation count with the per-BFS cost to derive the bound.*

## Relation to Ford-Fulkerson
*What does Edmonds-Karp fix about generic Ford-Fulkerson?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
