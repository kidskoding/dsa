/** Problem 7 — Valid Anagram. See {@code PROBLEM_SET.md}. */
class Problem7 {

	/**
	 * Returns whether {@code t} is an anagram of {@code s} — same characters with the same
	 * multiplicities. Compare frequency maps (or count up for s and down for t).
	 *
	 * @param s the first string, non-null
	 * @param t the second string, non-null
	 * @return {@code true} if t is an anagram of s
	 */
	boolean isAnagram(String s, String t) {
		// TODO: build a char-count map for s, decrement for t, confirm all counts are zero.
		throw new UnsupportedOperationException("TODO: implement isAnagram");
	}
}
