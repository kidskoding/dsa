/** Problem 4 — Contains Duplicate. See {@code PROBLEM_SET.md}. */
class Problem4 {

	/**
	 * Returns whether any value in {@code nums} appears at least twice. Add values to a hash set
	 * during a single scan and report a collision the moment one is found.
	 *
	 * @param nums the input array, non-null
	 * @return {@code true} if any value repeats
	 */
	boolean containsDuplicate(int[] nums) {
		// TODO: scan once, returning true when a value is already in the seen-set.
		throw new UnsupportedOperationException("TODO: implement containsDuplicate");
	}
}
