/** Problem 21 — Balanced Ledger Window. See {@code PROBLEM_SET.md}. */
class Problem21 {

	/**
	 * Returns the length of the longest contiguous range of {@code changes} that sums to exactly
	 * zero, or 0 if none exists. Track each prefix sum's earliest index in a hash map.
	 *
	 * @param changes the daily net changes, non-null
	 * @return the length of the longest zero-sum range
	 */
	int longestZeroSum(int[] changes) {
		// TODO: map prefixSum -> earliest index; a repeat marks a zero-sum range between positions.
		throw new UnsupportedOperationException("TODO: implement longestZeroSum");
	}
}
