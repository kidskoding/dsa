/** Problem 8 — First Unique Character. See {@code PROBLEM_SET.md}. */
class Problem8 {

	/**
	 * Returns the index of the first character in {@code s} that appears exactly once, or {@code -1}
	 * if none exists. Build a frequency map, then scan left to right for the first count-1 character.
	 *
	 * @param s the input string, non-null
	 * @return the index of the first unique character, or -1
	 */
	int firstUniqChar(String s) {
		// TODO: count characters, then return the first index whose character has count 1.
		throw new UnsupportedOperationException("TODO: implement firstUniqChar");
	}
}
