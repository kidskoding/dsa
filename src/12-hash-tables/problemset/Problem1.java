/** Problem 1 — Two Sum. See {@code PROBLEM_SET.md}. */
class Problem1 {

	/**
	 * Returns the indices {@code [i, j]} with {@code i < j} of the two elements of {@code nums} that
	 * sum to {@code target}. Exactly one solution exists. Use a hash map from value to index.
	 *
	 * @param nums the input array, non-null, with exactly one valid pair
	 * @param target the target sum
	 * @return the two 0-based indices, smaller first
	 */
	int[] twoSum(int[] nums, int target) {
		// TODO: for each x, check whether (target - x) was already seen in a value->index map.
		throw new UnsupportedOperationException("TODO: implement twoSum");
	}
}
