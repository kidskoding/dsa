/** Problem 22 — Stable Rune Lines. See {@code PROBLEM_SET.md}. */
class Problem22 {

	/**
	 * Returns how many lines are stable. A line is stable if, after counting each distinct rune's
	 * occurrences, every nonzero count is unique (no two distinct runes share a count).
	 *
	 * @param lines the scroll lines, non-null
	 * @return the number of stable lines
	 */
	int countStable(String[] lines) {
		// TODO: per line, count runes; the line is stable iff its count values have no duplicates.
		throw new UnsupportedOperationException("TODO: implement countStable");
	}
}
