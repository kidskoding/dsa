/** Problem 19 — Elf Gift Pairing. See {@code PROBLEM_SET.md}. */
class Problem19 {

	/**
	 * Returns the number of distinct unordered value pairs {@code (a, b)} with {@code a < b},
	 * {@code b - a == d}, and both present in {@code values}. Count value pairs, not index pairs.
	 *
	 * @param values the joy values, non-null, possibly with duplicates
	 * @param d the required positive difference
	 * @return the number of distinct qualifying value pairs
	 */
	int countPairs(int[] values, int d) {
		// TODO: put distinct values in a set; for each value a, count if (a + d) is present.
		throw new UnsupportedOperationException("TODO: implement countPairs");
	}
}
