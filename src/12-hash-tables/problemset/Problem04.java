import java.util.List;

/** Tier 1 — Problem 04: Deduplicate preserving order. */
public class Problem04 {

	/**
	 * Returns a new list containing the elements of {@code nums} with duplicates removed, keeping the
	 * first occurrence order. Use a hash set to track seen values.
	 *
	 * @param nums the input array, non-null
	 * @return the deduplicated values in first-seen order
	 */
	public List<Integer> deduplicate(int[] nums) {
		// TODO: append each value the first time it is seen, tracking seen values in a hash set.
		throw new UnsupportedOperationException("TODO: implement deduplicate");
	}
}
