/** Problem 20 — Handle Team Signatures. See {@code PROBLEM_SET.md}. */
class Problem20 {

	/**
	 * Groups handles by signature — the case-insensitive multiset of their letters, ignoring digits
	 * and non-letter characters — and returns the size of the largest team, or 0 if there are none.
	 *
	 * @param handles the handles, non-null
	 * @return the size of the largest team
	 */
	int largestTeam(String[] handles) {
		// TODO: build a canonical letter-signature per handle; count group sizes in a map, take max.
		throw new UnsupportedOperationException("TODO: implement largestTeam");
	}
}
