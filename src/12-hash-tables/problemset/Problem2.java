import java.util.Map;

/** Problem 2 — Character Frequency Count. See {@code PROBLEM_SET.md}. */
class Problem2 {

	/**
	 * Returns a map from each distinct character in {@code s} to its occurrence count. Characters
	 * not present in {@code s} are absent from the map.
	 *
	 * @param s the input string, non-null, lowercase letters
	 * @return a map of character to frequency
	 */
	Map<Character, Integer> frequencies(String s) {
		// TODO: scan s once, incrementing the count for each character in a hash map.
		throw new UnsupportedOperationException("TODO: implement frequencies");
	}
}
