/** Tier 1 — Problem 02: Two-sum existence. */
public class Problem02 {

	/**
	 * Returns whether some pair of distinct indices in {@code nums} sums to {@code target}. Use a
	 * hash set of complements for an {@code O(n)} solution.
	 *
	 * @param nums the input array, non-null
	 * @param target the target sum
	 * @return {@code true} if such a pair exists
	 */
	public boolean hasTwoSum(int[] nums, int target) {
		// TODO: scan once, checking for target - nums[i] in a seen-set before adding nums[i].
		throw new UnsupportedOperationException("TODO: implement hasTwoSum");
	}
}
