/** Problem 18 — Lone Odd-Count Code. See {@code PROBLEM_SET.md}. */
class Problem18 {

	/**
	 * Returns the single part code in {@code log} whose occurrence count is odd; every other code
	 * appears an even number of times. Tally counts in a hash map (or toggle set membership).
	 *
	 * @param log the combined scan log, non-null with exactly one odd-count code
	 * @return the lone odd-count code
	 */
	int loneOddCode(int[] log) {
		// TODO: count codes (or XOR-toggle a set); return the code whose final count is odd.
		throw new UnsupportedOperationException("TODO: implement loneOddCode");
	}
}
