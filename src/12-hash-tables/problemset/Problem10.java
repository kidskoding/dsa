/** Problem 10 — Ransom Note. See {@code PROBLEM_SET.md}. */
class Problem10 {

	/**
	 * Returns whether {@code ransomNote} can be built from the letters of {@code magazine}, using
	 * each magazine letter at most once. Count available letters and spend them across the note.
	 *
	 * @param ransomNote the note to build, non-null
	 * @param magazine the available letters, non-null
	 * @return {@code true} if the note can be constructed
	 */
	boolean canConstruct(String ransomNote, String magazine) {
		// TODO: count magazine letters in a map; decrement per note letter, failing if any runs out.
		throw new UnsupportedOperationException("TODO: implement canConstruct");
	}
}
