/** Problem 23 — Submission Verdict Streak. See {@code PROBLEM_SET.md}. */
class Problem23 {

	/**
	 * Returns the length of the longest contiguous run of {@code ids} with no repeated id. Use a
	 * sliding window with a hash map from id to its last seen index.
	 *
	 * @param ids the stream of problem ids, non-null
	 * @return the length of the longest distinct-id run
	 */
	int longestDistinctRun(int[] ids) {
		// TODO: slide a window; on a repeat inside the window, advance left past the prior index.
		throw new UnsupportedOperationException("TODO: implement longestDistinctRun");
	}
}
