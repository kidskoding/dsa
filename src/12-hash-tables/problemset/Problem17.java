/** Problem 17 — Longest Consecutive Sequence. See {@code PROBLEM_SET.md}. */
class Problem17 {

	/**
	 * Returns the length of the longest run of consecutive integers in {@code nums}. Put all values
	 * in a hash set; start counting only at run starts ({@code x - 1} absent) for O(n) time.
	 *
	 * @param nums the input array, non-null
	 * @return the length of the longest consecutive run
	 */
	int longestConsecutive(int[] nums) {
		// TODO: add values to a set; from each run start, walk upward counting the run length.
		throw new UnsupportedOperationException("TODO: implement longestConsecutive");
	}
}
