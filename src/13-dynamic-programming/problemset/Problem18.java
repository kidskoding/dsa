/**
 * Problem 18 — Partition Equal Subset Sum.
 *
 * <p>Decide whether {@code nums} can be split into two subsets with equal sums.
 */
class Problem18 {

	/**
	 * Returns whether {@code nums} can be partitioned into two equal-sum subsets.
	 *
	 * @param nums the positive integers
	 * @return {@code true} if an equal split exists
	 */
	boolean canPartition(int[] nums) {
		// TODO: total must be even; subset-sum to total/2.
		throw new UnsupportedOperationException("TODO: implement canPartition");
	}
}
