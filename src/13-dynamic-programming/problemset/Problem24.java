/**
 * Problem 24 — Target Sum.
 *
 * <p>Count the ways to assign {@code +}/{@code -} signs to {@code nums} so the expression equals
 * {@code target}.
 */
class Problem24 {

	/**
	 * Returns the number of sign assignments of {@code nums} that evaluate to {@code target}.
	 *
	 * @param nums the non-negative integers
	 * @param target the target value
	 * @return the number of valid sign assignments
	 */
	int findTargetSumWays(int[] nums, int target) {
		// TODO: reduce to subset-sum count: P with sum (total + target) / 2.
		throw new UnsupportedOperationException("TODO: implement findTargetSumWays");
	}
}
