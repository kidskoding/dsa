/**
 * Problem 11 — Maximum Product Subarray.
 *
 * <p>Compute the maximum product of any non-empty contiguous subarray, tracking both running max
 * and min so a negative factor can flip them.
 */
class Problem11 {

	/**
	 * Returns the maximum product over all non-empty contiguous subarrays of {@code nums}.
	 *
	 * @param nums the input array (non-empty)
	 * @return the maximum subarray product
	 */
	int maxProduct(int[] nums) {
		// TODO: track curMax and curMin; on each element consider x, curMax*x, curMin*x.
		throw new UnsupportedOperationException("TODO: implement maxProduct");
	}
}
