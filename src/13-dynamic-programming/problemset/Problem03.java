/**
 * Problem 03 — House Robber.
 *
 * <p>Given an array of non-negative house values along a street, compute the maximum total you can
 * take without ever taking two adjacent houses.
 */
public class Problem03 {

	/**
	 * Returns the maximum total value obtainable without taking two adjacent houses.
	 *
	 * @param values the non-negative house values
	 * @return the maximum non-adjacent total (0 for an empty street)
	 */
	public int maxLoot(int[] values) {
		// TODO: dp[i] = max(dp[i-1], dp[i-2] + values[i]).
		throw new UnsupportedOperationException("TODO: implement maxLoot");
	}
}
