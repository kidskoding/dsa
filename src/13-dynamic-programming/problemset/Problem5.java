/**
 * Problem 5 — Subset Sum Feasibility.
 *
 * <p>Decide whether some subset of {@code nums} sums to exactly {@code t}.
 */
class Problem5 {

	/**
	 * Returns whether some subset of {@code nums} sums to exactly {@code t}.
	 *
	 * @param nums the positive integers
	 * @param t the target sum ({@code t >= 0})
	 * @return {@code true} if a subset sums to {@code t}
	 */
	boolean canSum(int[] nums, int t) {
		// TODO: boolean dp[s]; dp[0] = true; for each x, set dp[s] |= dp[s - x] going downward.
		throw new UnsupportedOperationException("TODO: implement canSum");
	}
}
