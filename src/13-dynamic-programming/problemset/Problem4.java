/**
 * Problem 4 — Unique Grid Paths.
 *
 * <p>Count distinct right/down paths from the top-left to bottom-right of an {@code m x n} grid.
 */
class Problem4 {

	/**
	 * Returns the number of distinct right/down paths across an {@code m x n} grid.
	 *
	 * @param m the number of rows ({@code m >= 1})
	 * @param n the number of columns ({@code n >= 1})
	 * @return the number of distinct paths
	 */
	int uniquePaths(int m, int n) {
		// TODO: dp[i][j] = dp[i - 1][j] + dp[i][j - 1]; first row/column are 1.
		throw new UnsupportedOperationException("TODO: implement uniquePaths");
	}
}
