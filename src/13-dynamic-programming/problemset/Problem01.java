/**
 * Problem 01 — Maximum Subarray Sum.
 *
 * <p>Given an integer array, compute the maximum sum of any non-empty contiguous subarray (Kadane's
 * algorithm framed as a DP).
 */
public class Problem01 {

	/**
	 * Returns the maximum sum over all non-empty contiguous subarrays of {@code nums}.
	 *
	 * @param nums the input array (non-empty)
	 * @return the maximum subarray sum
	 */
	public int maxSubarraySum(int[] nums) {
		// TODO: best ending here = max(nums[i], bestEndingHere + nums[i]); track the global max.
		throw new UnsupportedOperationException("TODO: implement maxSubarraySum");
	}
}
