/**
 * Problem 10 — Perfect Squares.
 *
 * <p>Return the least number of perfect squares that sum to {@code n}.
 */
class Problem10 {

	/**
	 * Returns the minimum count of perfect squares summing to {@code n}.
	 *
	 * @param n the target ({@code n >= 1})
	 * @return the fewest perfect squares
	 */
	int numSquares(int n) {
		// TODO: dp[i] = min over squares j*j <= i of dp[i - j*j] + 1; dp[0] = 0.
		throw new UnsupportedOperationException("TODO: implement numSquares");
	}
}
