/**
 * Problem 05 — Subset Sum Feasibility.
 *
 * <p>Given an array of positive integers and a target {@code t}, decide whether some subset sums
 * exactly to {@code t}.
 */
public class Problem05 {

	/**
	 * Returns whether some subset of {@code nums} sums exactly to {@code target}.
	 *
	 * @param nums the positive integers
	 * @param target the target sum ({@code target >= 0})
	 * @return true if a subset sums to {@code target}
	 */
	public boolean canSum(int[] nums, int target) {
		// TODO: boolean DP reachable[s] over sums; reachable[0] = true.
		throw new UnsupportedOperationException("TODO: implement canSum");
	}
}
