/**
 * Problem 1 — Climbing Stairs.
 *
 * <p>Each move climbs 1 or 2 steps. Count the distinct ways to climb {@code n} stairs.
 */
class Problem1 {

	/**
	 * Returns the number of distinct ways to climb {@code n} stairs taking 1 or 2 steps at a time.
	 *
	 * @param n the number of stairs ({@code n >= 1})
	 * @return the number of distinct climbing sequences
	 */
	int climbStairs(int n) {
		// TODO: ways[i] = ways[i - 1] + ways[i - 2]; ways[0] = ways[1] = 1.
		throw new UnsupportedOperationException("TODO: implement climbStairs");
	}
}
