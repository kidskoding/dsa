/**
 * Problem 06 — Minimum Falling Path Sum.
 *
 * <p>Given a square matrix, find the minimum sum of a path from the top row to the bottom row,
 * moving each step to the cell directly below or diagonally below.
 */
public class Problem06 {

	/**
	 * Returns the minimum falling-path sum through the square matrix {@code grid}.
	 *
	 * @param grid a square matrix (non-empty, {@code n x n})
	 * @return the minimum top-to-bottom falling-path sum
	 */
	public int minFallingPathSum(int[][] grid) {
		// TODO: dp[r][c] = grid[r][c] + min of the three reachable cells in the row above.
		throw new UnsupportedOperationException("TODO: implement minFallingPathSum");
	}
}
