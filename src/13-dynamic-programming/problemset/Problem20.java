/**
 * Problem 20 — Minimum Path Sum.
 *
 * <p>Find the minimum sum right/down path from the top-left to bottom-right of {@code grid}.
 */
class Problem20 {

	/**
	 * Returns the minimum path sum across {@code grid}, moving only right or down.
	 *
	 * @param grid the non-negative grid (non-empty)
	 * @return the minimum path sum
	 */
	int minPathSum(int[][] grid) {
		// TODO: dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1]).
		throw new UnsupportedOperationException("TODO: implement minPathSum");
	}
}
