/**
 * Problem 28 — Minimum Workshop Split.
 *
 * <p>Split positive part weights into two groups minimizing the absolute difference of their sums.
 */
class Problem28 {

	/**
	 * Returns the minimum absolute difference between the two group sums.
	 *
	 * @param parts the positive part weights
	 * @return the minimum achievable difference
	 */
	int minDifference(int[] parts) {
		// TODO: subset-sum reachable up to total; pick reachable s closest to total/2; answer = total - 2s.
		throw new UnsupportedOperationException("TODO: implement minDifference");
	}
}
