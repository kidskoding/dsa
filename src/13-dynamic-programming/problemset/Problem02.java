/**
 * Problem 02 — Climbing Stairs.
 *
 * <p>You can climb 1 or 2 steps at a time. Given {@code n} stairs, count the number of distinct
 * ways to reach the top.
 */
public class Problem02 {

	/**
	 * Returns the number of distinct ways to climb {@code n} stairs taking 1 or 2 steps at a time.
	 *
	 * @param n the number of stairs ({@code n >= 0})
	 * @return the number of distinct ways (1 when {@code n == 0})
	 */
	public int countWays(int n) {
		// TODO: ways(n) = ways(n-1) + ways(n-2); ways(0) = ways(1) = 1.
		throw new UnsupportedOperationException("TODO: implement countWays");
	}
}
