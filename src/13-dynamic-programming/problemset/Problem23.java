/**
 * Problem 23 — Maximal Square.
 *
 * <p>Find the area of the largest all-ones square in a binary matrix.
 */
class Problem23 {

	/**
	 * Returns the area of the largest square of {@code 1}s in {@code matrix}.
	 *
	 * @param matrix the binary matrix of {@code 0}/{@code 1} values (non-empty)
	 * @return the maximal square area
	 */
	int maximalSquare(int[][] matrix) {
		// TODO: dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) when cell is 1.
		throw new UnsupportedOperationException("TODO: implement maximalSquare");
	}
}
