/**
 * Problem 3 — House Robber.
 *
 * <p>Maximize the loot from a row of houses without robbing two adjacent houses.
 */
class Problem3 {

	/**
	 * Returns the maximum amount robbable from {@code nums} without taking two adjacent houses.
	 *
	 * @param nums the non-negative house values
	 * @return the maximum total loot
	 */
	int rob(int[] nums) {
		// TODO: dp[i] = max(dp[i - 1], dp[i - 2] + nums[i]).
		throw new UnsupportedOperationException("TODO: implement rob");
	}
}
