# Rod Cutting

## The Problem Setup
*Given a price-per-length table, what are we maximizing over a rod of length \\( n \\)?*

## First-Cut Recurrence
*Write \\( r(n) \\) as a \\( \max \\) over the length of the first piece.*

## Naive Recursion Blowup
*Why does the unmemoized recursion explode, and roughly how fast?*

## Memoized vs Bottom-Up
*Contrast the two implementations and their \\( O(n^2) \\) cost.*

## Recovering the Cuts
*What auxiliary table lets you print the actual cut lengths?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
