/**
 * Problem 02 — Prefix existence.
 *
 * <p>Add {@link #startsWith(String)} returning whether any inserted key has the given prefix, in \(
 * O(L) \) time.
 */
public class Problem02 {

	/**
	 * Inserts {@code word} into the trie.
	 *
	 * @param word the key to insert
	 */
	public void insert(String word) {
		// TODO: create the path of trie nodes and mark the terminal node.
		throw new UnsupportedOperationException("TODO: implement insert");
	}

	/**
	 * Returns whether any inserted key begins with {@code prefix}.
	 *
	 * @param prefix the prefix to test
	 * @return true if at least one inserted key has the prefix
	 */
	public boolean startsWith(String prefix) {
		// TODO: return whether the path for prefix exists, ignoring terminal marks.
		throw new UnsupportedOperationException("TODO: implement startsWith");
	}
}
