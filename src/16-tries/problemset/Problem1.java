/**
 * Problem 1 — Insert and Search Keys.
 *
 * <p>Process a stream of {@code "insert word"} / {@code "search word"} commands against a trie and
 * return the boolean result of every {@code search}, in order, as {@code "true"} / {@code "false"}.
 */
class Problem1 {

	/**
	 * Runs the operation stream and collects one {@code "true"}/{@code "false"} per {@code search}.
	 *
	 * @param operations each {@code "insert <word>"} or {@code "search <word>"}, lowercase letters
	 * @return the result of each {@code search} command, in order
	 */
	String[] run(String[] operations) {
		// TODO: build a trie; insert marks a terminal node, search checks the end-of-word flag.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
