/**
 * Problem 18 — Maximum XOR Pair.
 *
 * <p>The input is an array of non-negative integers as decimal numerals. Insert each number's
 * fixed-width binary form into a binary trie and return the maximum pairwise XOR as a string.
 */
class Problem18 {

	/**
	 * Returns the maximum value of {@code nums[i] XOR nums[j]} over all pairs, as a string.
	 *
	 * @param nums non-negative integers given as decimal numerals
	 * @return the maximum pairwise XOR
	 */
	String run(String[] nums) {
		// TODO: insert binary representations into a trie; greedily prefer opposite bits per number.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
