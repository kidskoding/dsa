/**
 * Problem 2 — Prefix Existence.
 *
 * <p>Process a stream of {@code "insert word"} / {@code "prefix p"} commands and report, for each
 * prefix query, whether any inserted key begins with {@code p}.
 */
class Problem2 {

	/**
	 * Runs the operation stream and collects one {@code "true"}/{@code "false"} per {@code prefix}.
	 *
	 * @param operations each {@code "insert <word>"} or {@code "prefix <p>"}, lowercase letters
	 * @return the result of each {@code prefix} query, in order
	 */
	String[] run(String[] operations) {
		// TODO: build a trie; a prefix query succeeds when the path for p exists, terminal or not.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
