/**
 * Problem 05 — Longest common prefix.
 *
 * <p>Given a set of strings inserted into a trie, return the longest prefix common to all of them.
 */
public class Problem05 {

	/**
	 * Inserts {@code word} into the trie.
	 *
	 * @param word the key to insert
	 */
	public void insert(String word) {
		// TODO: create the path of trie nodes and mark the terminal node.
		throw new UnsupportedOperationException("TODO: implement insert");
	}

	/**
	 * Returns the longest prefix shared by every inserted key.
	 *
	 * @return the longest common prefix, or the empty string if none is shared
	 */
	public String longestCommonPrefix() {
		// TODO: descend from the root while each node has exactly one child and is
		// not itself a terminal key.
		throw new UnsupportedOperationException("TODO: implement longestCommonPrefix");
	}
}
