/**
 * Problem 01 — Insert and search.
 *
 * <p>Build a trie supporting {@link #insert(String)} and {@link #search(String)} where search
 * returns {@code true} only for complete inserted keys. Both run in \( O(L) \) time for a key of
 * length \( L \).
 */
public class Problem01 {

	/**
	 * Inserts {@code word} as a complete key.
	 *
	 * @param word the key to insert
	 */
	public void insert(String word) {
		// TODO: create a path of trie nodes and mark the terminal node.
		throw new UnsupportedOperationException("TODO: implement insert");
	}

	/**
	 * Returns whether {@code word} is a complete inserted key.
	 *
	 * @param word the key to search for
	 * @return true only for complete keys, not prefixes
	 */
	public boolean search(String word) {
		// TODO: follow the path and check the end-of-word mark.
		throw new UnsupportedOperationException("TODO: implement search");
	}
}
