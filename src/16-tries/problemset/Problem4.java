/**
 * Problem 4 — Delete a Key.
 *
 * <p>Process a stream of {@code "insert word"} / {@code "delete word"} / {@code "search word"}
 * commands, pruning dead nodes on deletion, and report the result of every {@code search}.
 */
class Problem4 {

	/**
	 * Runs the operation stream and collects one {@code "true"}/{@code "false"} per {@code search}.
	 *
	 * @param operations each {@code "insert <word>"}, {@code "delete <word>"}, or {@code "search <word>"}
	 * @return the result of each {@code search} command, in order
	 */
	String[] run(String[] operations) {
		// TODO: delete unsets the terminal flag and prunes nodes that become childless and non-terminal.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
