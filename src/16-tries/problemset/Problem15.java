/**
 * Problem 15 — Segmenting the Signal Stream.
 *
 * <p>The first element is the signal; the rest are vocabulary chunks. Decide whether the signal can
 * be split into a contiguous sequence of vocabulary chunks (each reusable any number of times).
 */
class Problem15 {

	/**
	 * Returns {@code "true"} if the signal segments fully into vocabulary chunks, else {@code "false"}.
	 *
	 * @param input element 0 is the signal; elements 1.. are the vocabulary chunks
	 * @return {@code "true"} or {@code "false"}
	 */
	String run(String[] input) {
		// TODO: store the vocabulary in a trie; run a memoised scan following trie edges to find splits.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
