/**
 * Problem 5 — Longest Common Prefix.
 *
 * <p>Insert every word into a trie and descend from the root while exactly one child exists and the
 * node is not the end of a shorter word, returning the longest prefix common to all words.
 */
class Problem5 {

	/**
	 * Returns the longest prefix shared by every string in {@code words}.
	 *
	 * @param words the input strings (lowercase letters; may be empty strings)
	 * @return the longest common prefix, or {@code ""} if none is shared
	 */
	String run(String[] words) {
		// TODO: build a trie, then walk down while branching is unambiguous and no shorter word ends.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
