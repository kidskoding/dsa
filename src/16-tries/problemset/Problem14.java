/**
 * Problem 14 — Prefix Tally at the Lexicon Archive.
 *
 * <p>The input holds a word block, a {@code "---"} separator, then a query block. For each query
 * prefix, report how many archived words begin with it.
 */
class Problem14 {

	/**
	 * Returns one count per query prefix, as strings, in query order.
	 *
	 * @param lines the word block, a single {@code "---"} separator, then the query prefixes
	 * @return the number of archived words beginning with each query prefix
	 */
	String[] run(String[] lines) {
		// TODO: build a trie with a passthrough count per node; answer each query at its prefix node.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
