/**
 * Problem 9 — Map Sum Pairs.
 *
 * <p>Process a stream of {@code "insert key val"} / {@code "sum prefix"} commands. An insert sets or
 * overwrites a key's value; a sum returns the total value of all keys starting with the prefix.
 */
class Problem9 {

	/**
	 * Runs the operation stream and collects one total per {@code sum} query, as a string.
	 *
	 * @param operations each {@code "insert <key> <val>"} or {@code "sum <prefix>"}
	 * @return the sum answer for each {@code sum} query, in order
	 */
	String[] run(String[] operations) {
		// TODO: store each key's value at its terminal node; a sum totals values across the subtree.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
