/**
 * Problem 11 — Word Break.
 *
 * <p>The first element is the target string; the rest form the dictionary. Decide whether the
 * target can be segmented into a sequence of one or more dictionary words (each reusable).
 */
class Problem11 {

	/**
	 * Returns {@code "true"} if the target can be fully segmented into dictionary words, else
	 * {@code "false"}.
	 *
	 * @param input element 0 is the target string; elements 1.. are the dictionary words
	 * @return {@code "true"} or {@code "false"}
	 */
	String run(String[] input) {
		// TODO: store the dictionary in a trie; run a memoised left-to-right scan following trie edges.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
