/**
 * Problem 8 — Replace Words With Roots.
 *
 * <p>The first element is a space-separated sentence; the rest are dictionary roots. Replace each
 * word by the shortest root that is a prefix of it, leaving unmatched words unchanged.
 */
class Problem8 {

	/**
	 * Returns the sentence with each word replaced by its shortest matching root.
	 *
	 * @param input element 0 is the sentence; elements 1.. are the roots (lowercase letters)
	 * @return the rewritten sentence, words joined by single spaces
	 */
	String run(String[] input) {
		// TODO: build a trie of roots; for each word walk until the first terminal node.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
