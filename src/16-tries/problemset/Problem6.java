/**
 * Problem 6 — Implement Trie (Prefix Tree).
 *
 * <p>Process a stream of {@code "insert word"} / {@code "search word"} / {@code "startsWith prefix"}
 * commands and report the boolean result of every {@code search} and {@code startsWith}, in order.
 */
class Problem6 {

	/**
	 * Runs the operation stream, emitting one {@code "true"}/{@code "false"} per query command.
	 *
	 * @param operations each {@code "insert <word>"}, {@code "search <word>"}, or {@code "startsWith <prefix>"}
	 * @return the result of each {@code search} and {@code startsWith}, in order
	 */
	String[] run(String[] operations) {
		// TODO: insert builds a path; search checks the terminal flag; startsWith checks path existence.
		throw new UnsupportedOperationException("TODO: implement run");
	}
}
