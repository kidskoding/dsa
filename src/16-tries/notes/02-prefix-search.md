# Prefix Search

## Walking to the Prefix Node
*How do you locate the node where a query prefix ends?*

## startsWith vs. Exact Match
*Why is prefix existence cheaper than full-key lookup?*

## Subtree Enumeration
*How do you collect all keys sharing a prefix from that node?*

## Counting Keys with a Prefix
*How can per-node counters answer prefix counts in \\( O(L) \\)?*

## Output-Sensitive Cost
*Why does enumeration cost depend on the number of results?*

## Implementation
```java
// implement from scratch here
```

## My Notes
*Fill this in as you learn*
