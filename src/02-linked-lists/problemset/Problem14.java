/**
 * Problem 14 — Train car partition. Reorders the list so every node with value strictly less than
 * {@code x} precedes every node with value >= x, preserving relative order within each group,
 * mutating links only.
 */
class Problem14 {

	/**
	 * Partitions the list around the pivot value {@code x}.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @param x the pivot value
	 * @return the head of the partitioned list
	 */
	ListNode partition(ListNode head, int x) {
		// TODO: build a "less than x" chain and a "greater or equal" chain, then join them.
		throw new UnsupportedOperationException("TODO: implement Problem14.partition");
	}
}
