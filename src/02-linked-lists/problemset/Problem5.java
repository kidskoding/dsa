/**
 * Problem 5 — Remove duplicates from a sorted list. Deletes nodes so each value appears once,
 * keeping the list sorted, and returns the head.
 */
class Problem5 {

	/**
	 * Removes duplicate values from a sorted list.
	 *
	 * @param head the head of a sorted list (may be {@code null})
	 * @return the head of the de-duplicated list
	 */
	ListNode deleteDuplicates(ListNode head) {
		// TODO: walk the list, skipping a node whose value equals its predecessor's.
		throw new UnsupportedOperationException("TODO: implement Problem5.deleteDuplicates");
	}
}
