/**
 * Problem 10 — Intersection of two lists. Returns the first node the two lists share by reference,
 * or {@code null} if they do not intersect, in O(m + n) time and O(1) extra space.
 */
class Problem10 {

	/**
	 * Returns the first shared node of the two lists.
	 *
	 * @param a the head of the first list
	 * @param b the head of the second list
	 * @return the intersection node, or {@code null}
	 */
	ListNode getIntersectionNode(ListNode a, ListNode b) {
		// TODO: walk both pointers, switching heads at the end so they align on the shared tail.
		throw new UnsupportedOperationException("TODO: implement Problem10.getIntersectionNode");
	}
}
