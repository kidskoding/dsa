/**
 * Problem 4 — Merge two sorted lists. Splices the nodes of two ascending lists into one ascending
 * list, reusing the existing nodes, and returns its head.
 */
class Problem4 {

	/**
	 * Merges two ascending sorted lists into one.
	 *
	 * @param a the head of the first sorted list
	 * @param b the head of the second sorted list
	 * @return the head of the merged sorted list
	 */
	ListNode merge(ListNode a, ListNode b) {
		// TODO: thread a dummy head and repeatedly attach the smaller front node.
		throw new UnsupportedOperationException("TODO: implement Problem4.merge");
	}
}
