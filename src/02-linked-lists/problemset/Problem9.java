/**
 * Problem 9 — Swap nodes in pairs. Swaps every two adjacent nodes by relinking (not by swapping
 * values) and returns the new head; a trailing odd node stays in place.
 */
class Problem9 {

	/**
	 * Swaps each adjacent pair of nodes.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return the head of the list after pairwise swaps
	 */
	ListNode swapPairs(ListNode head) {
		// TODO: use a dummy head and relink each pair (first, second) as (second, first).
		throw new UnsupportedOperationException("TODO: implement Problem9.swapPairs");
	}
}
