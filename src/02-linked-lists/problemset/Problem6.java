/**
 * Problem 6 — Remove the N-th node from the end. Removes the n-th node from the end in a single pass
 * and returns the new head.
 */
class Problem6 {

	/**
	 * Removes the {@code n}-th node from the end of the list.
	 *
	 * @param head the head of the list
	 * @param n the 1-based position from the end
	 * @return the head of the modified list
	 */
	ListNode removeNthFromEnd(ListNode head, int n) {
		// TODO: lead one pointer n nodes ahead so the gap marks the node to unlink.
		throw new UnsupportedOperationException("TODO: implement Problem6.removeNthFromEnd");
	}
}
