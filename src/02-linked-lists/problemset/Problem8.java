/**
 * Problem 8 — Odd-even linked list. Groups nodes at odd positions before nodes at even positions
 * (1-based by index), preserving relative order, in O(1) extra space.
 */
class Problem8 {

	/**
	 * Reorders the list so odd-position nodes precede even-position nodes.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return the head of the reordered list
	 */
	ListNode oddEvenList(ListNode head) {
		// TODO: thread an odd chain and an even chain, then splice even after odd.
		throw new UnsupportedOperationException("TODO: implement Problem8.oddEvenList");
	}
}
