/**
 * Problem 1 — Reverse a singly linked list. Given the head of a singly linked list, return the head
 * of the reversed list, rewiring the {@code next} pointers in O(n) time and O(1) extra space.
 */
class Problem1 {

	/**
	 * Reverses the linked list and returns the new head.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return the head of the reversed list
	 */
	ListNode reverse(ListNode head) {
		// TODO: walk the list, flipping each node's next pointer to its predecessor.
		throw new UnsupportedOperationException("TODO: implement Problem1.reverse");
	}
}
