/**
 * Problem 3 — Detect a cycle. Returns whether the list contains a cycle, using O(1) extra space
 * (Floyd's slow/fast pointers).
 */
class Problem3 {

	/**
	 * Returns {@code true} if the list contains a cycle.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return whether a cycle exists
	 */
	boolean hasCycle(ListNode head) {
		// TODO: advance slow by one and fast by two; a meeting means a cycle.
		throw new UnsupportedOperationException("TODO: implement Problem3.hasCycle");
	}
}
