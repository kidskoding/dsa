/**
 * Problem 18 — Next greater node. For each node front to back, reports the value of the next
 * strictly greater node, or 0 if none, in O(n) time using a monotonic stack.
 */
class Problem18 {

	/**
	 * Returns, for each position, the next strictly greater value, or 0.
	 *
	 * @param head the head of a non-empty list
	 * @return an array of next-greater values indexed by 0-based position
	 */
	int[] nextLargerNodes(ListNode head) {
		// TODO: read values into an array, then resolve next-greater with a decreasing-value stack.
		throw new UnsupportedOperationException("TODO: implement Problem18.nextLargerNodes");
	}
}
