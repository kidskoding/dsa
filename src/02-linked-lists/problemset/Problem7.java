/**
 * Problem 7 — Palindrome linked list. Returns whether the list's values read the same forwards and
 * backwards, in O(n) time and O(1) extra space.
 */
class Problem7 {

	/**
	 * Returns whether the list is a palindrome.
	 *
	 * @param head the head of a non-empty list
	 * @return whether the values form a palindrome
	 */
	boolean isPalindrome(ListNode head) {
		// TODO: find the middle, reverse the second half, then compare the two halves.
		throw new UnsupportedOperationException("TODO: implement Problem7.isPalindrome");
	}
}
