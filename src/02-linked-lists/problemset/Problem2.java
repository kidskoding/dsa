/**
 * Problem 2 — Find the middle node. Returns the middle node of a non-empty list; for even length,
 * returns the second of the two middle nodes, using a single slow/fast pass.
 */
class Problem2 {

	/**
	 * Returns the middle node of the list.
	 *
	 * @param head the head of a non-empty list
	 * @return the middle node
	 */
	ListNode middle(ListNode head) {
		// TODO: advance a fast pointer two steps for every slow step; slow lands on the middle.
		throw new UnsupportedOperationException("TODO: implement Problem2.middle");
	}
}
