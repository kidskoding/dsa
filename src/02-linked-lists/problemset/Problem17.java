/**
 * Problem 17 — Remove duplicates from a sorted list II. Deletes every node that has a duplicate
 * value, leaving only originally-distinct values, in O(n) time; returns the new head.
 */
class Problem17 {

	/**
	 * Removes all nodes that have a duplicate value.
	 *
	 * @param head the head of a sorted list (may be {@code null})
	 * @return the head of the list of distinct values
	 */
	ListNode deleteDuplicates(ListNode head) {
		// TODO: use a dummy head; skip whole runs of equal values when the run length exceeds one.
		throw new UnsupportedOperationException("TODO: implement Problem17.deleteDuplicates");
	}
}
