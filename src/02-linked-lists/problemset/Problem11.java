/**
 * Problem 11 — Reorder list. Reorders L0 -> L1 -> ... -> Ln in place to L0 -> Ln -> L1 -> Ln-1 ...,
 * changing only node links, and returns the new head.
 */
class Problem11 {

	/**
	 * Reorders the list in place and returns its head.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return the head of the reordered list
	 */
	ListNode reorderList(ListNode head) {
		// TODO: split at the middle, reverse the second half, then weave the two halves.
		throw new UnsupportedOperationException("TODO: implement Problem11.reorderList");
	}
}
