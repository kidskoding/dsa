/**
 * Problem 19 — Merge zero segments. The list starts and ends with a 0 node with no adjacent zeros;
 * merges each segment strictly between two zeros into one node holding its sum and drops the zeros.
 */
class Problem19 {

	/**
	 * Compacts each between-zeros segment into a single summed node.
	 *
	 * @param head the head of the list (starts and ends with a 0 node)
	 * @return the head of the compacted list
	 */
	ListNode mergeNodes(ListNode head) {
		// TODO: accumulate values until the next zero, then emit one node holding the running sum.
		throw new UnsupportedOperationException("TODO: implement Problem19.mergeNodes");
	}
}
