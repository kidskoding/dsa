/**
 * Problem 20 — Sort a linked list. Returns the list sorted ascending in O(n log n) time, ideally
 * with O(1) auxiliary space via bottom-up merge sort.
 */
class Problem20 {

	/**
	 * Sorts the list in ascending order.
	 *
	 * @param head the head of the list (may be {@code null})
	 * @return the head of the sorted list
	 */
	ListNode sortList(ListNode head) {
		// TODO: split into runs of growing size, merge adjacent runs bottom-up until sorted.
		throw new UnsupportedOperationException("TODO: implement Problem20.sortList");
	}
}
