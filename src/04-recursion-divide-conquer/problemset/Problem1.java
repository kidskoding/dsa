/**
 * Problem 1 — Sum of Digits. Given a non-negative integer {@code n}, return the sum of its base-10
 * digits using recursion (no loops).
 */
class Problem1 {

	/**
	 * Returns the sum of the base-10 digits of {@code n}.
	 *
	 * @param n a non-negative integer
	 * @return the sum of the digits of {@code n}
	 */
	int sumOfDigits(int n) {
		// TODO: base case n < 10; otherwise (n % 10) + sumOfDigits(n / 10).
		throw new UnsupportedOperationException("TODO: implement sumOfDigits");
	}
}
