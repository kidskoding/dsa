import java.util.List;

/**
 * Problem 18 — Generate Parentheses. Given {@code n} pairs, generate all well-formed parenthesis
 * strings using backtracking. Any order is accepted.
 */
class Problem18 {

	/**
	 * Returns every well-formed string of {@code n} parenthesis pairs, in any order.
	 *
	 * @param n the number of parenthesis pairs
	 * @return all valid combinations
	 */
	List<String> generateParenthesis(int n) {
		// TODO: add '(' while opens remain; add ')' while it stays balanced; recurse.
		throw new UnsupportedOperationException("TODO: implement generateParenthesis");
	}
}
