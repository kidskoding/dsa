/**
 * Problem 5 — Greatest Common Divisor. Compute {@code gcd(a, b)} with the recursive Euclidean
 * algorithm, with {@code gcd(a, 0) = a} and {@code gcd(0, 0) = 0}.
 */
class Problem5 {

	/**
	 * Returns the greatest common divisor of {@code a} and {@code b}.
	 *
	 * @param a a non-negative integer
	 * @param b a non-negative integer
	 * @return the greatest common divisor of {@code a} and {@code b}
	 */
	long gcd(long a, long b) {
		// TODO: base case b == 0 -> a; otherwise gcd(b, a % b).
		throw new UnsupportedOperationException("TODO: implement gcd");
	}
}
