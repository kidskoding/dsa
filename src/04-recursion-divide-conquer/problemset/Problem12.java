import java.util.List;

/**
 * Problem 12 — Subsets. Given an array of distinct integers, return all subsets (the power set)
 * built with recursion/backtracking. Order of subsets does not matter.
 */
class Problem12 {

	/**
	 * Returns every subset of {@code nums}, in any order.
	 *
	 * @param nums an array of distinct integers
	 * @return a list of all subsets of {@code nums}
	 */
	List<List<Integer>> subsets(int[] nums) {
		// TODO: at each index, recurse including and excluding the element.
		throw new UnsupportedOperationException("TODO: implement subsets");
	}
}
