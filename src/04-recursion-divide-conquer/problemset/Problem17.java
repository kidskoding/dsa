/**
 * Problem 17 — Count Inversions. Count pairs {@code (i, j)} with {@code i < j} and {@code a[i] >
 * a[j]} in {@code O(n log n)} by piggybacking on the merge step of merge sort.
 */
class Problem17 {

	/**
	 * Returns the number of inversions in {@code a}.
	 *
	 * @param a the array to analyze
	 * @return the count of index pairs {@code (i, j)} with {@code i < j} and {@code a[i] > a[j]}
	 */
	long countInversions(int[] a) {
		// TODO: merge-sort; when taking a right element early, add the remaining left count.
		throw new UnsupportedOperationException("TODO: implement countInversions");
	}
}
