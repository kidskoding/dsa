/**
 * Problem 7 — Pow(x, n). Raise the floating-point base {@code x} to the integer power {@code n}
 * using fast exponentiation in {@code O(log |n|)}, handling negative exponents via the reciprocal.
 */
class Problem7 {

	/**
	 * Returns {@code x} raised to the power {@code n}.
	 *
	 * @param x the base
	 * @param n the integer exponent, possibly negative
	 * @return {@code x^n}
	 */
	double myPow(double x, int n) {
		// TODO: square the half-power; for negative n raise the reciprocal of x.
		throw new UnsupportedOperationException("TODO: implement myPow");
	}
}
