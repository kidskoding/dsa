/**
 * Problem 20 — Median of Two Sorted Arrays. Return the median of the union of two ascending-sorted
 * arrays in {@code O(log(min(m, n)))} by binary-searching a partition of the shorter array.
 */
class Problem20 {

	/**
	 * Returns the median of the combined multiset of {@code a} and {@code b}.
	 *
	 * @param a an array sorted in non-decreasing order
	 * @param b an array sorted in non-decreasing order
	 * @return the median of all elements of {@code a} and {@code b}
	 */
	double findMedianSortedArrays(int[] a, int[] b) {
		// TODO: binary-search a split of the shorter array so left side <= right side.
		throw new UnsupportedOperationException("TODO: implement findMedianSortedArrays");
	}
}
