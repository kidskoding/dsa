/**
 * Problem 05 — Greatest common divisor. Compute {@code gcd(a, b)} with the recursive Euclidean
 * algorithm.
 */
public class Problem05 {

	/**
	 * Returns the greatest common divisor of {@code a} and {@code b} using the recursive Euclidean
	 * algorithm.
	 *
	 * @param a the first non-negative integer
	 * @param b the second non-negative integer
	 * @return {@code gcd(a, b)}
	 */
	public long gcd(long a, long b) {
		// TODO: base case b == 0 returns a; otherwise gcd(b, a % b).
		throw new UnsupportedOperationException("TODO: implement gcd");
	}
}
