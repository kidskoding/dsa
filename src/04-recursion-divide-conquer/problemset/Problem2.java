/**
 * Problem 2 — Recursive Power. Compute {@code b} raised to the non-negative integer exponent {@code
 * e} using exponentiation by squaring in {@code O(log e)} multiplications.
 */
class Problem2 {

	/**
	 * Returns {@code b} raised to the power {@code e}.
	 *
	 * @param b the base
	 * @param e a non-negative exponent
	 * @return {@code b^e}
	 */
	long power(long b, int e) {
		// TODO: base case e == 0 -> 1; square the half-power, multiply once more if e is odd.
		throw new UnsupportedOperationException("TODO: implement power");
	}
}
